# Shipping Management 개발 가이드

- 문서 버전: 2.0.1
- 대상 진입점: `orders.html`
- 현재 단계: 구매계획·로컬 복구·명시적 클라우드 저장 Pilot
- 업무 소유자: 배송·발주 운영 관리자
- 기술 소유 범위: `orders.html`, `orderFulfillmentEngine.js`, `orderFulfillmentWorkbook.js`, `code.gs`의 Shipping 전용 action/시트

## 1. 제품 목적

Shipping Management는 관리자가 주문현황과 창고별재고 Excel을 수작업으로 대조하던 시간을 줄이는 독립형 ONEAPP 애플리케이션이다.

현재 Pilot의 목표는 다음 네 단계로 업무를 단순화하는 것이다.

1. `주문현황 Excel`과 `창고별재고 Excel`을 선택한다.
2. 상품코드 기준으로 출고 가능 수량과 추가 구매 필요량을 분석한다.
3. 발주관리에서 상품코드별 구매 거래처를 입력하고 로컬 자동복구 또는 명시적 클라우드 revision으로 보존한다.
4. 원본·계산·검증 내역을 함께 담은 `미출고현황 Excel` 또는 ERP 형식의 `구매업로드 Excel`을 내려받는다.

계산 결과는 관리자 판단을 대신하지 않는다. 관리자는 `발주관리`, `주문원본`, `창고별 재고`, `검증결과`를 통해 원본과 처리 결과를 직접 비교한 뒤 실제 출고·구매를 결정한다.

## 2. MVP 범위와 비범위

### Pilot 범위

- 입력 파일 2개만 브라우저에서 읽는다.
- 필수 열, 상품코드, 수량, 재고 중복을 검증한다.
- 상품코드로만 주문과 재고를 연결한다.
- 동일 상품 주문은 주문현황 Excel 행 순서대로 배정한다.
- `전재고 → 서울 1차 구매잔량 → 추가 구매 필요` 순서로 배정한다.
- 분석 요약과 미리보기 탭을 표시한다.
- 아래 6개 시트를 가진 `미출고현황 Excel`을 생성한다.
  - `발주관리`
  - `미출고현황`
  - `상품별요약`
  - `검증결과`
  - `창고별 재고`
  - `주문원본`
- 내부 작업공간은 `shipping-workspace/v2` JSON 계약으로 직렬화 가능하게 유지한다.
- 발주관리의 구매 입력은 상품코드 단위로 미출고현황·상품별요약·일반 Excel에 동기화한다.
- 분석 workspace와 구매 입력은 IndexedDB에 자동저장하며, localStorage에는 복구 포인터와 메타데이터만 저장한다.
- 사용자가 명시적으로 저장한 계획만 `ONEAPP_SHIPPING_PURCHASE_PLAN_V1` cloud revision으로 공유한다.
- 원본 A:T 계약을 보존하는 별도 `구매업로드_YYYYMMDD.xlsx`를 생성한다.
- 헤더의 ONEAPP NEXUS 브랜드 전체는 `dashboard.html`로 이동하는 링크이며, cloud·로컬 관리 컨트롤은 헤더 환경설정 modal에서 제공한다.
- 환경설정 modal은 키보드 포커스를 내부에 유지하고 ESC·닫기·배경 클릭으로 닫으며, 로컬 복구 초기화는 명시 확인 뒤에만 실행한다.

### 명시적 비범위

- 주문 수기 등록·수정·삭제
- 로그인·권한 변경
- MerchOps 마스터 또는 다른 운영 데이터 직접 쓰기
- 자동 클라우드 전송 또는 revision 자동 삭제
- Lot별 재고·구매·FIFO
- 출고완료 처리
- 판매전표 초안 생성
- 긴급도·거래처 우선순위 자동 추론
- 상품명 유사도 또는 이미지로 상품코드 자동 확정

## 3. 입력·출력 Excel 계약

### 주문현황 Excel

- 실제 기준 시트: `미판매현황`처럼 제목행 다음에 열 머리글이 있는 표
- 머리글 탐색: 앞쪽 30행에서 필수 열 조합을 찾는다.
- 필수 열: `품목코드`, `품목명`, `규격`, `수량`, `적요`, `적요1`, `거래처`, `그룹`
- 선택 보존 열: `일자-No.`, `담당`, `단위`, `재고`, `단가`
- 데이터행 기준: 상품코드가 있거나 상품명·수량 등 주문 필드가 있는 행
- 차단 오류:
  - 필수 열 누락
  - 상품 데이터가 있으나 상품코드 누락
  - 수량이 숫자가 아니거나 음수
- 상품코드는 공백을 제거하거나 숫자로 다시 해석하지 않고 앞뒤 공백만 제거한 텍스트 식별자로 보존한다.
- `일자-No.`는 일련번호를 제거한 순수 일자로 해석하며, 같은 분석에 둘 이상의 기준일이 있으면 구매업로드를 차단한다.
- `적요` 또는 `적요1`에 값이 있으면 발주관리 대상에 포함하고 동일 상품코드 안에서 원문 값을 중복 제거해 보존하며 의미·긴급도·우선순위를 추론하지 않는다.

### 창고별재고 Excel

- 실제 기준 시트: `재고현황`처럼 제목행 다음에 열 머리글이 있는 표
- 머리글 탐색: 앞쪽 30행에서 필수 열 조합을 찾는다.
- 필수 열: `품목코드`, `품목명`, `규격`, `1창고`, `3서울`, `4전송`
- 선택 보존 열: `사용`, `단위`, `수량`, `2전송`, `7진영`, `기본`, `전송`, `창고`
- 공란 재고 셀은 계산 가능수량 0으로 해석하되 원본 시트에서는 공란을 그대로 보존한다.
- 차단 오류:
  - 필수 열 누락
  - 상품 데이터가 있으나 상품코드 누락
  - `1창고`, `3서울`, `4전송`의 숫자 해석 실패
  - 동일 상품코드가 둘 이상 존재

### 출력 Excel

- 파일명: `미출고현황_YYYY-MM-DD.xlsx`
- 상품코드는 모든 결과·원본 시트에서 텍스트로 저장한다.
- 계산 시트에는 제목, 기준 설명, 필터, 고정 머리글, 열 너비, 수량 서식, 상태 색상을 적용한다.
- `주문원본`과 `창고별 재고`는 입력값 비교를 위한 감사 원본이며, 출력 계산값으로 원본을 덮어쓰지 않는다.
- `발주관리`는 첫 시트이며 `purchaseNeed > 0`, 재고정보 없음, 적요/적요1 존재 상품의 합집합을 상품코드당 한 행으로 표시한다.
- 추가 구매가 필요한 본 상품 바로 뒤에는 정규화 코드 앞 6자리가 같은 재고 상품을 참고행으로 표시한다. 참고행은 주문·배정·추가구매 합계와 구매업로드에서 제외한다.
- 구매값이 정확히 `대체` 또는 `소분`일 때만 구매업로드에서 제외한다. 공란과 오타·확장 문구는 일반 거래처명으로 처리한다.
- 거래처(단가)는 주문 원본의 거래처+단가 쌍을 평균내지 않고 중복 제거해 표시한다.
- `발주관리`에서 미매칭 상품의 구매수량은 확정하지 않으며 구매업로드를 생성하지 않는다.
- 계산값은 같은 입력과 같은 행 순서에서 항상 같은 결과를 내야 한다.

### 구매업로드 Excel

- 시트명은 `구매입력`, 열 순서는 원본 양식의 A:T를 유지한다.
- A 일자는 8자리 텍스트 `YYYYMMDD`, E 입고창고는 텍스트 `01`, F 거래유형은 공란, I 코드는 텍스트다.
- L 수량은 계산된 추가 구매 필요 수량의 숫자형이고 M 단가/구매가는 숫자 0이다.
- 대상은 재고가 매칭되고 `purchaseNeed > 0`인 본 상품행이며 참고행과 exact `대체`/`소분`은 제외한다.
- 원본 `구매업로드.xlsx`는 읽기 전용 기준 양식이며 생성·검증 과정에서 덮어쓰지 않는다.

## 4. 배정 규칙과 용어

| 용어 | 정의 |
|---|---|
| 전재고 | 창고별재고의 `1창고` 원값 중 출고에 사용할 수 있는 `MAX(0, 1창고)` 수량 |
| 서울 1차 구매분 | 창고별재고의 `3서울` 원값 |
| 1차 판매·전송 | 창고별재고의 `4전송` 원값. 현재 판매·전송은 음수로 기록된다. |
| 서울 1차 구매잔량 | `MAX(0, 3서울 + 4전송)` |
| 추가 구매 필요 | 주문수량에서 전재고 배정과 서울 1차 구매분 배정을 뺀 비음수 부족분 |
| 재고정보 없음 | 주문 상품코드가 재고 파일에 존재하지 않는 상태 |

배정 순서는 다음과 같다.

1. 동일 상품 주문을 주문현황 Excel의 행 순서대로 처리한다.
2. 남은 전재고에서 가능한 만큼 배정한다.
3. 부족하면 남은 서울 1차 구매잔량에서 배정한다.
4. 그래도 부족하면 추가 구매 필요량으로 기록한다.
5. 한 주문행에 사용한 재고는 이후 주문행에서 다시 사용하지 않는다.
6. 상품명은 표시용이며 매칭에 사용하지 않는다.
7. 재고정보가 없으면 전재고와 서울 잔량을 0으로 확정하거나 주문수량을 구매수량으로 확정하지 않는다.

## 5. 아키텍처 경계와 소유권

- `orderFulfillmentEngine.js`는 파일 독립적인 정규화·검증·배정·대사 로직의 소유자다.
- `orderFulfillmentWorkbook.js`는 Shipping Management 결과 통합문서 구조와 표시 형식의 소유자다.
- `orders.html`은 파일 선택, 상태 표시, 결과 미리보기, 다운로드 흐름만 소유한다.
- 로컬 복구는 전용 IndexedDB `ONEAPPShippingManagementDB`의 `workspaces` store를 사용한다. `sourceFingerprint`와 `schemaVersion`이 일치하지 않는 자료는 자동복구하지 않는다.
- cloud 계획은 `ShippingPlanStaging`, append-only `ShippingPlanHistory`, 확정 `ShippingPlanIndex`를 사용한다. index가 유일한 조회 경계다.
- Shipping cloud action은 전용 `ONEAPP_SHIPPING_PLAN_ACCESS_TOKEN`을 사용하고 DataOps/Master/History/Config 저장계약과 격리한다.
- `coreEngine.js`를 로드하거나 수정하지 않는다.
- MerchOps와 DataOps의 데이터·업무 의미·함수키·저장 흐름을 변경하지 않는다.
- NEXUS Operations의 기존 `orders.html` 경로만 사용하며, 다른 운영 화면을 Shipping Management의 종속성으로 만들지 않는다.

## 6. 통합 계약과 향후 경계

향후 기능은 각 소유 애플리케이션과 별도 승격 검토를 거쳐야 한다.

### 저장구조 대안 검토

- DataOps와 같은 A/B latest slot 방식은 확정 전환이 단순하지만 planId별 과거 revision을 불변 이력으로 유지할 수 없어 제외했다.
- 하나의 history 시트만 매번 전체 스캔하는 방식은 append-only에는 맞지만 목록 메타 조회 비용과 payload 경계 검증이 불명확해 제외했다.
- 전용 staging 검증 → append-only history → index 최종 확정을 선택했다. index만 list/get 조회 경계로 사용하므로 history append 뒤 index 실패로 생긴 orphan은 노출되지 않고 이전 latest가 보존된다.
- 파일 바이너리나 전체 workspace를 localStorage에 저장하는 방식은 용량·충돌·손상 범위가 커서 제외하고, 전용 IndexedDB에는 workspace를, localStorage에는 schema가 붙은 포인터/메타데이터만 저장한다.

| 통합 대상 | 향후 입력 계약 | Shipping Management 출력/제약 |
|---|---|---|
| 클라우드 공유 | `ONEAPP_SHIPPING_PURCHASE_PLAN_V1`, `shipping-workspace/v2`, 작성자·원본 해시·전용 token | 명시적 save/list/get만 허용하고 이전 확정 revision을 덮어쓰거나 자동 삭제하지 않는다. |
| MerchOps | 상품코드 기반의 승인된 제품 표시정보를 읽기 전용으로 참조 | 마스터를 직접 수정하지 않으며 매칭 실패를 상품명으로 보정하지 않는다. |
| DataOps | 승인된 재고·구매 결과의 버전과 기준일을 읽기 전용으로 참조 | DataOps의 Lot 의미나 마감 결과를 재정의하지 않고 현재 집계재고 모델과 명시적으로 구분한다. |
| Smart Parser | 원본 열 인식 후보와 신뢰도 | 필수 열 매핑을 자동 확정하지 않고 관리자 확인 전에는 계산 계약을 바꾸지 않는다. |
| 이미지 인식 | 송장·라벨 이미지에서 추출한 후보 코드와 근거 영역 | 후보를 검토 대상으로만 전달하고 이미지 결과만으로 상품코드를 확정하지 않는다. |
| 구매·Lot 데이터 | 구매 ID, Lot ID, 입고일, 잔량, 소유 시스템, 기준시각 | `shipping-workspace/v2` 이상의 별도 계약으로 도입하며 집계재고 MVP 결과와 혼합하지 않는다. |
| 출고확정 | 승인자, 확정수량, 확정시각, 멱등키 | 분석 결과와 출고확정 기록을 분리하며 중복 확정을 막는 쓰기 API가 승인된 뒤 도입한다. |
| 판매전표 초안 | 확정 출고행과 거래처·단가 검증 결과 | 초안만 생성하고 원장 반영은 소유 시스템의 검토·승인 절차를 따른다. |

## 7. 검증, 테스트, 롤백

### 검증 규칙

- 차단 검증을 통과하기 전에는 분석 버튼을 활성화하지 않는다.
- 미매칭·메모는 분석을 차단하지 않지만 건수와 상세를 발주관리·검증결과에 표시한다.
- 재고 중복 상품코드는 차단한다.
- 모든 매칭 주문행에서 `주문수량 = 전재고 배정 + 서울 배정 + 추가 구매 필요`가 성립해야 한다.
- 상품별 주문 합계와 주문행 배정 합계가 일치해야 한다.
- 추가 구매 필요량은 음수가 될 수 없다.
- 원본 행 수와 출력 원본 행 수를 대사한다.
- 실제 기준 파일과 엣지 케이스 자동 테스트를 모두 실행한다.
- 브라우저에서 파일 선택, 분석, 탭 전환, 다운로드 버튼 상태, 반응형 레이아웃을 확인한다.

### 테스트 기준

- 동일 상품 여러 주문행의 입력순서 배정
- 전재고와 서울 잔량 소진
- 음수 `4전송` 반영
- 재고 미매칭 처리
- 재고 중복코드 차단
- `적요`·`적요1` 합집합과 무손실 중복 제거
- 앞 6자리 참고그룹과 합계·구매업로드 제외
- 구매 exact 제외와 오타/공란 포함
- 로컬 복구 fingerprint 격리와 손상 fallback
- cloud index 실패 orphan 비노출, 이전 latest 보존, hash/count 재검증, token/action 격리
- 추가 구매 필요량 비음수
- 주문·상품별 배정 대사
- 6개 필수 출력 시트 존재와 발주관리 첫 위치, 상품코드 텍스트 보존
- 구매업로드 A:T 헤더·굵은 필수표시·텍스트/숫자 타입·round-trip 보존

### 롤백 규칙

- 문제가 있으면 NEXUS의 Pilot 진입을 비활성화하고 해당 PR을 revert한다.
- 로컬 복구 자료는 사용자 명시 초기화 전까지 유지한다. append-only cloud revision은 롤백 시 삭제하지 않으며 index에 확정된 이전 revision을 다시 조회할 수 있다.
- Shipping 전용 cloud action과 시트만 비활성화하고 DataOps/Master/History/Config 계약은 그대로 유지한다.
- 생성된 통합문서는 검토용 비권위 산출물이며 운영 원본의 백업으로 사용하지 않는다.
- PR 단위 revert와 전용 local/cloud/workbook 재검증을 기본 롤백 경로로 사용한다.
