# ORDER Q 대체출고·시스템 메시지 개발명세 v1.0

- 작업 ID: `ORDERQ-SUBSTITUTE-SHIPMENT-20260904`
- 개발 분류: 중요 개발(주문 작업본 변경 및 로컬·Cloud 저장/복구 영향)
- 작성일: 2026-09-04
- 기준 저장소: `https://github.com/orderzoneapp-coder/oneapp.git`
- 기준 `origin/main`: `d44bbda357268289269574aa8f7b36333e013be5`
- 작업 브랜치: `codex/orderq-substitute-shipment-20260904`
- 작업 경로: `C:\Users\USER\Documents\ChatGPT\검증 PM\work\oneapp-orderq-substitute-shipment-20260904`
- 시작 상태: 기준 SHA와 HEAD 일치, clean

## 1. 목적과 현재 문제

ORDER Q 창고재고 화면에서 다른 상품으로 출고할 거래처 주문을 매일 반복 처리한다. 드래그앤드롭은 마우스 이동 시간이 길고 저사양 환경에서 반응이 불안정하므로 사용하지 않는다. 현재는 거래처별 실제 출고상품을 빠르게 변경하고 나중에 변경 결과를 확인할 영구 이력이 없다.

## 2. 확정 결과

1. 창고재고의 `정보` 거래처 칩을 한 번 클릭해 대체할 주문을 선택한다.
2. `Ctrl`을 누른 채 실제 출고할 상품 행을 클릭하면 확인창 없이 즉시 대체출고한다.
3. 주문수량과 거래처 단가는 유지하고 실제 출고 상품코드·품명·규격·단위만 선택 상품으로 바꾼다.
4. 변경 직후 주문·재고·잔량·구매필요를 기존 엔진으로 다시 계산한다.
5. 우측 `시스템 메시지` 열에 원상품과 대체상품 양쪽 작업 결과, 작업자와 시각을 자동 기록한다.
6. `Ctrl+Z`는 마지막 대체출고를 복원하되 기존 이력을 삭제하지 않고 취소 이력을 추가한다.
7. 원 주문상품은 주문행의 별도 요청상품 Snapshot과 원본 Excel matrix에 보존한다.

## 3. 데이터와 저장 경계

- 앱 소유 데이터: 기존 `shipping-workspace/v2` 작업본의 주문행, 계산 결과, additive `substitutionHistory`.
- 외부 읽기 데이터: 사용자가 선택한 주문현황·창고재고 Excel.
- 대체출고 이력 계약: `shipping-substitution-history/v1`; 이벤트는 append-only다.
- 저장: 기존 로컬 복구와 명시적 Cloud 완료본에 workspace 일부로 함께 저장한다.
- 서버 필수 시점: 기존과 동일하게 사용자가 Cloud 새 버전 저장을 실행할 때뿐이다.
- 외부 장애 시 유지 기능: 선택, 대체출고, 재계산, 시스템 메시지, 로컬 임시저장.
- 다른 앱 연결: 추가하지 않는다. MerchOps·DataOps·공통 Runtime 저장소를 읽거나 쓰지 않는다.

## 4. UI/UX 계약

- 기존 NEXUS 공통 UI 자산과 ORDER Q 표 구조를 그대로 사용한다.
- 신규 모달·패널·드래그앤드롭을 추가하지 않는다.
- 선택 칩은 텍스트 외 테두리·`aria-pressed`로 구분한다.
- Ctrl 입력 중 선택 가능한 행을 명확히 표시하고, 같은 상품 선택은 변경하지 않는다.
- `시스템 메시지`는 창고재고 표의 맨 오른쪽 열이며 최신 작업부터 표시한다.
- 일반/다크 화면에서 상태 의미를 유지하고 출력 Excel 형식은 변경하지 않는다.
- 모바일은 Ctrl 조작 대상이 아니며 기존 조회·스크롤·출력 기능을 보존한다.

## 5. 허용·금지 범위

### 변경 허용

- `orderFulfillmentEngine.js`
- `orderops/list.html` 및 공개 호환 미러 `orderops_list.html`
- 관련 자동 테스트
- 본 개발명세, `SHIPPING_MANAGEMENT_GUIDANCE.md`, `APP_ARCHITECTURE.md`
- 변경 계약을 설명하는 `app-manifest.json` 메타데이터

### 변경 금지

- 원본 Excel matrix 덮어쓰기
- 재고 원본 수량 수정
- 다른 앱 DB 또는 원시 저장소 직접 쓰기
- 자동 Cloud 저장·자동 출고확정·판매전표 생성
- 부분수량 분할 대체와 BOX/EA 환산
- 대체상품·단가 자동 추천 또는 자동 판단

## 6. 완료조건과 검증

- 한 주문 칩 선택 후 Ctrl+다른 상품 행 클릭으로 주문이 대상 상품에 이동한다.
- 주문수량·단가가 유지되고 원상품 요청 Snapshot과 append-only 이력이 남는다.
- 원상품·대체상품의 주문수량, 잔량, 구매필요가 다시 계산된다.
- 시스템 메시지가 양쪽 상품에 표시되고 JSON round-trip 후에도 유지된다.
- 같은 상품, 없는 상품, 없는 주문행은 변경 없이 이해 가능한 오류를 반환한다.
- Ctrl+Z 복원 후 계산과 메시지가 일치하며 취소 이력이 추가된다.
- 기존 Shipping 자동 테스트와 신규 엔진/UI 계약 테스트가 통과한다.
- 실제 브라우저에서 일반/다크, 선택 강조, Ctrl+클릭, Ctrl+Z, 로컬 저장·복구, console error 0을 확인한다.
- 공통 자산 실패 조건에서 기존 로컬 분석·대체출고 Core가 독립 실행됨을 정적 의존성 경계로 확인한다.

## 7. 롤백

본 작업의 엔진 함수, workspace additive 필드 소비, 두 ORDER Q 화면의 선택 동작·시스템 메시지 열과 관련 테스트를 한 커밋으로 되돌린다. 기존 `shipping-workspace/v2`, 원본 Excel, 이미 저장된 Cloud revision은 삭제하지 않는다. 구버전은 알 수 없는 additive 필드를 무시하고 기존 주문 작업본을 계속 읽는다.

## 8. 구현·검증 기록

- 엔진: 대체출고, 전체 재계산, 요청상품 Snapshot, append-only 적용·취소 이력 구현.
- 화면: 거래처 칩 선택, Ctrl+상품 행 클릭, Esc 선택 해제, Ctrl+Z 복원, 우측 시스템 메시지 열 구현.
- 자동 검증: 신규 대체출고 계약, 기존 Shipping 기준자료(주문 86행·재고 280행), 작업복구·날짜·설정, Cloud 실패 주입, 공통 UI 18개 화면, 테마·인쇄 브라우저 E2E 통과.
- 수동 브라우저 검증: 대체출고·재계산·양쪽 메시지, Ctrl+Z, 로컬 복구, 일반/다크 화면 확인. Console error와 warning 0건.
- 출력 경계: 통합 Excel 구조와 값 계약은 변경하지 않음.
