# SmartInput 입력 양식 매핑·저장 UX 작업 기록

- 작업일: 2026-09-06
- 분류: 일반 개발
- 대상 앱: SmartInput
- 원격 저장소: `https://github.com/orderzoneapp-coder/oneapp.git`
- 최초 로컬 기준 SHA: `fce4a628b6c68fa25fdc427a96d5abdb119ab194`
- 최초 확인한 `origin/main` SHA: `44851c183cd97ff6ad19cd3d0a748e587a91cb07`
- 2026-09-07 재기준화 `origin/main` SHA: `247bcbbd085986edb6e76a66af0bd564c2682235`
- 적용 문서: `AGENTS.md` 2.3.4, `APP_ARCHITECTURE.md`, `app-manifest.json`, `roles/PM.md`, `roles/DEVELOPER.md`

## 목적과 완료조건

신규 입력 양식의 추천 매핑을 확정 매핑과 명확히 구분하고, 추천 승인 및 검색 결과의 키보드 선택을 즉시 확정한다. 양식 저장이 차단될 때 관리자가 누락 항목을 일일이 찾지 않도록 문제 열을 강조하고, 연결 대상과 중복 상대 열을 표시하며 첫 문제 열의 매핑 창을 자동으로 연다. 기본 입력 필드는 사용자가 지정한 10개 필드와 순서로 구성한다.

완료조건은 다음과 같다.

- 추천 상태가 승인 전 상태로 표시되고 승인 즉시 `MAPPED`와 `reviewed=true`로 저장된다.
- 검색창에서 Enter 또는 방향키로 결과에 진입하고 방향키 이동 후 Enter로 지정할 수 있다.
- 미승인 추천, 미결정, 중복, 삭제된 대상과 재검토 필요 상태를 구분해 정확한 열을 안내한다.
- 저장 검증 실패 시 원본 열 전체 맥락을 보존한 채 문제 열을 강조하고, 각 열을 확정할 때 남은 문제 열 목록을 즉시 갱신한다.
- 기본 입력 필드는 코드, 품명, 규격(기본), 수량, 단가, 공급가, 간단설명(품위), 지시사항, 출고가 (공지), 판매no. 순서로 표시한다.
- 원본 행·셀 값, 입력 양식 저장 계약과 기존 전표 투영 결과를 변경하지 않는다.
- 단위 테스트와 대표 브라우저 E2E가 통과한다.

## 현재 상태와 목표 상태

- 현재 운영 상태: 추천과 확정의 시각 구분이 약하고, 저장 실패 시 중복 또는 삭제된 대상이라는 일반 문구만 표시한다.
- 목표 상태: 관리자가 추천을 명시적으로 승인하고 검색 결과를 키보드로 지정하며, 저장 실패 원인을 해당 열에서 즉시 수정할 수 있다.

## 정책·경계

- 실행 방식: `LOCAL_OPERATION`
- SmartInput 화면, 입력 양식 매핑 검증과 기본 입력 필드 구성만 변경한다.
- IndexedDB 버전, Store, 데이터 마이그레이션, 공통 Runtime과 다른 앱 계약은 변경하지 않는다.
- 일반·다크 화면과 모바일 폭은 기존 공통 UI 토큰과 반응형 구조를 유지한다.
- 원격 최신 변경과 겹치는 SmartInput 소스는 최신 `origin/main`에 재기준화한 뒤 회귀검증한다.

## 2026-09-07 최신 main 통합 결정

- PR #537에서 문제 열 강조, 첫 오류 자동 이동, 이전·다음 탐색, 수정 직후 재검증과 저장 재개가 더 완성된 단일 검증 흐름으로 병합됐다. 기존 작업의 별도 `mappingIssueFilter`와 문제 열 전용 필터는 중복 구현하지 않고 PR #537 검증 흐름을 유지한다.
- 이 작업에서는 아직 운영에 없는 추천 매핑 명시 승인, 검색 결과 키보드 선택·즉시 확정, 추천·미결정 저장 차단과 지정 10개 기본 필드·순서만 최신 검증 흐름에 통합한다.
- PR #538~#544의 작업바, 입력형 기본 보기, 보고서, 매칭사전, 로그인과 주문·출고 파이프라인 변경을 그대로 보존한다.
- 재기준화 후에도 실행 방식은 `LOCAL_OPERATION`이며 DB version, Store, migration, 원본 셀·행·위치 서명과 전표 저장 계약은 변경하지 않는다.

## 구현·검증 결과

- 기준 SHA: `247bcbbd085986edb6e76a66af0bd564c2682235` (`origin/main`, PR #544 병합 커밋)
- 추천 카드는 승인 전임을 표시하며 승인 또는 검색 결과 선택 시 `MAPPED`, `reviewed=true`로 즉시 확정한다.
- 검색창 Enter/위·아래 방향키와 결과 목록 Enter를 지원한다.
- `RECOMMENDATION_APPROVAL_REQUIRED`, `UNDECIDED_COLUMN`, 중복, 삭제 대상, 재검토 필요 상태가 신규 양식 저장을 차단하고 PR #537의 단계별 검증 안내를 이어서 사용한다.
- 주문 입력 기본 필드는 코드, 품명, 규격(기본), 수량, 단가, 공급가, 간단설명(품위), 지시사항, 출고가 (공지), 판매no. 순서로 구성했다.
- 별도 문제 열 필터는 원본 열 맥락을 숨기므로 제외하고 PR #537의 전체 열 유지·문제 열 강조·자동 이동·저장 재개 흐름을 보존했다.
- 정적 검증 통과: `test-smartinput-input-template-mapping`, `test-smartinput-settings-ux`, `test-smartinput-appheader-workspace`, `test-smartinput-independent-recovery`, `test-smartinput-v2-unresolved-review-ui`.
- 브라우저 E2E 통과: 입력 양식 매핑, SmartInput 전체 보호 작업공간, 환경설정 UX, 미해결 검토 UI, 입력목록 검색.
