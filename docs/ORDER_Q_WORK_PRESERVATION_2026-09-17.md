# ORDER Q 재분석·파일 교체 작업값 보존

- 기준: PR #622 병합본 `5cd99df2b6ec57bf2cdbc2601b858e6f1b2f0c54`.
- 목적: 독립 연속작업 검사에서 발견된 두 작업 유실만 국소 교정. 기존 계산·수불·단위·NEXUS 교정은 유지.
- 변경 범위: 두 출고관리 HTML 진입점, 계산 엔진의 작업본 재계산/원본 참조 분리, 직접 회귀와 CI.
- 제외: 3영역, API/주문 묶음 확대, 공식 원장·저장 schema 변경, 타 앱 업무/UI 변경. 워크북 계산식은 변경하지 않음.

## 동작

1. 기본 **출고분석**은 현재 작업본 사본을 재계산하고 성공 시 교체한다. 수량·단가·공급가·재고 보정·구매처·대체 이력·참고 구매/판매 자료와 원본 근거를 유지한다.
2. 입력 파일 객체와 편집용 행/원본 행렬을 분리한다. 편집 순서에 따라 입력 자료가 오염되지 않는다.
3. 로컬/Cloud에서 복구한 작업본도 원본 파일을 다시 선택하지 않고 재분석할 수 있다. 기존 명시적 **현재 파일로 복구자료 교체**는 파일 기준 새 분석·검증 저장 경로를 유지한다.
4. 새 개별/자동분류/통합 Excel은 임시 후보에서 읽기·필수 열·행 오류 검사를 마친 후에만 교체한다. 실패·취소 시 기존 선택 자료·작업본·출력·복구 이력은 유지한다.
5. 통합파일의 정상 시트만 적용하고 잘못된 종류는 기존 입력을 유지하는 계약은 그대로다. 유효한 새 파일 교체는 기존처럼 이전 분석 결과를 비우고 새 분석을 준비한다. 서로 다른 원본에 이전 보정값을 자동 이월하지 않는다.
6. 읽기·분석 중 중복 실행/초기화를 차단한다. 작업 완료 또는 실패 후 다시 사용할 수 있다.

## 검증

- 실제 두 HTML 함수, 엔진, recovery JSON, XLSX 재개방을 사용하는 작업값 보존 검사.
- 재고 먼저/주문 먼저 편집, 숫자 0·공란·음수, 재분석 실패, 파일 읽기 실패·구조 오류·취소·정상 교체.
- 실제 Chromium에서 입력·수정·재분석·출력·IndexedDB 저장·새로고침 복구·읽기 실패를 연속 실행.
- 기존 계산 36개 및 Shipping reference 118주문/286재고, Cloud 실패안전·소유 경계 회귀.
- PR CI, 병합 main CI, Pages와 실제 제공 소스 일치는 각각 확인한다. 이 문서만으로 배포 성공을 주장하지 않는다.

로컬 실행 결과: 작업 보존 82/82, 기존 계산 36/36, Shipping reference 및 관련 7개 스크립트 PASS. 실제 Chromium의 연속 편집/재분석/파일 실패/IndexedDB SHA 검증 저장/새로고침/복구/6시트 출력과 기존 F05~F08·테마·인쇄 회귀 PASS, 페이지 console warning/error 0. Client safety PASS, repository validation 24/0, diff check PASS. 잘못된 구매 필수 열을 사용하던 기존 브라우저 fixture는 정상 구매처 열로 수정하고 구매처 보존 검증을 추가했다.

## 추가 승인 범위: NEXUS 이동·재시도

- PR 첫 CI의 NEXUS 브라우저 검사가 실패했고 재실행도 다른 history 단계에서 실패했다. 사용자의 추가 처리 지시에 따라 공통 이동 경계만 국소 수정했다. 타 앱의 업무 처리·저장소는 변경하지 않았다.
- 결정적 Node 재현: 이전 문서의 늦은 load가 새 대상의 HOST_READY를 소비하여 실제 대상 연결이 timeout된다. 실제 Chromium 계측에서는 APP_READY 직후 부모 이력을 확정하고 늦은 native load로 재연결하는 순서와 history 실패를 확인했다.
- 호스트 1.2.2는 정확한 대상 URL/Document로 HOST_READY를 제한하고, 같은 Document의 native load·APP_READY가 모두 도착한 뒤에만 이력을 확정한다. 동일 문서의 늦은 load는 무시하며 동일 URL의 새 문서는 별도 transition으로 이전 신호를 배제한다. timeout 기간과 보안 검사는 완화하지 않았다.
- Node는 늦은 load, 준비/로딩 순서 역전, 동일 URL의 문서 교체, 오래된 READY/ERROR/timeout을 검사한다. 실제 Chromium은 기존 뒤로가기/재시도, 42방향 이동, 7개 실제 앱, 화면/테마/인쇄를 유지한다. 실패 시에는 업무값 없이 연결 메타데이터만 출력한다.

## 후속 범위

- 창고 헤더·시트 별칭(F02/F03)은 별도 후속 교정이다.
- 모든 기본 앱 결함이 해소됐다는 판정이 아니다. 이번 완료 범위는 두 작업 보존 문제와 추가 승인된 NEXUS 연결 경합이다.
