# 오더리스트 운영 백업 복구

사용자 요청: 개발 중인 오더큐에 오류가 있어 오더리스트에서 우선 작업해야 하나, 백업 화면까지 업데이트되어 주문현황을 인식하지 못함. 사용자가 지정한 복구 주소는 `/orderops/list.html`.

- 시작 기준: `21eaf44d50883c68f6c40a6eca9d368df1eff09f`.
- 복구 기준: `13c83a23a330f3319ae67005f7ff0d67dfae4d54` (2026-09-17). 9월 16일 기본 흐름 복구와 수량·작업 보존 보정을 포함하고, 9월 18일 Excel 준비/매핑 개편 이전이다.
- 해당 화면과 계산·출력 모듈만 복구한다. 화면은 기준 소스 그대로이며 두 모듈 URL만 `orderops/stable/20260917/`에 고정한다. 기존 NEXUS host 연결을 유지한다.
- `orderops_list.html`, 루트 계산·출력 모듈, `/orderq/`, 서버, 공식 원장과 저장 데이터는 변경하지 않는다. 기존 브라우저 저장소 이름도 변경하지 않는다. 코드 의존성 분리이며 별도 데이터 백업이나 저장소 분리는 아니다.
- 원인 확인: #624와 #636 등은 기본 경로와 호환 경로를 함께 변경했다. 이전까지 두 경로는 같은 최신 계산·출력 모듈을 사용했다. 바탕화면의 최신 `미출고현황.xlsx`와 `창고별재고.xlsx`는 복구 및 현재 엔진에서 모두 읽힌다. 원래 브라우저의 실패 설정까지 재현한 것은 아니므로 특정 변경이 오류의 직접 원인이라고 단정하지 않는다.
- 복구한 화면은 주문현황과 창고재고를 선택한 뒤 출고분석하는 기존 흐름이다. 이후의 단독 자료 자동 표시, Excel 준비 패널, 재고변동 실사 화면은 복구 화면에 포함하지 않는다.
- 검증: 복구 기준과 소스 일치, 기준 시점 계산/작업 보존 검사, 실제 Chromium에서 주문·재고 입력과 분석·복구·출력, NEXUS host 연결을 확인한다. 최신 Excel UI 검사는 기존 최신 화면 `orderops_list.html`을 대상으로 유지한다.
- 재발 방지: `test-orderops-stable-rollback.mjs`가 고정 기준과 실제 복구 소스의 일치를 검사한다. 오더큐 개발만으로 이 백업 화면이나 고정 모듈을 함께 업데이트하지 않는다. 백업 변경은 사용자 요청으로 기준을 다시 정한 경우에만 수행한다.
- 되돌리기: 이 복구 PR의 소스 변경을 revert한다. 브라우저 자료·공식 원장·Cloud 데이터를 삭제하거나 과거화하지 않는다.

검증 결과: 복구 소스·모듈 일치, 기준 계산 36개와 복구 경로 작업 보존 42개, 최신 화면 작업 보존 49개, 최신 Excel 준비 54개, repository validation 24개, client safety가 통과했다. 실제 바탕화면 파일은 격리한 Chromium에서 주문 108행·재고 297행 입력, 출고분석, 주문표 표시, IndexedDB 저장, 새로 진입 후 선택 복구와 작업공간 완전 일치를 확인했다. 실행 중 예외는 0건이다. 복구 화면 Phase 6B 브라우저 검사와 NEXUS 7개 앱 42방향 이동, 유지한 최신 화면의 Excel 준비 및 재고변동 브라우저 회귀도 통과했다. 업무 파일과 상세 화면 자료는 로컬에만 보관한다.
