# NEXUS-DARK-SOFT-SURFACES-20260902-01 작업 기록

- 분류: 일반 개발
- 사용자 승인: NEXUS 홈에서 다크모드의 반복 라인을 줄인 텍스트 중심 컨셉을 먼저 구현하고, 아이보리 메모장 톤의 일반모드와 공통 헤더형 화면모드 전환을 추가해 두 모드를 검증한 뒤 다른 앱 적용은 별도로 결정
- 원격 저장소: `https://github.com/orderzoneapp-coder/oneapp.git`
- 기준 `origin/main`: `32a27851de752667a883b773a2908519bd5666db`
- 브랜치: `codex/darkmode-soft-surfaces-20260902`
- worktree: `C:\Users\USER\Documents\ChatGPT\NEXUS\work\oneapp-darkmode-soft-surfaces-20260902`
- 시작 상태: clean
- 적용 규범: `AGENTS.md` v2.3.4
- 적용 아키텍처: `APP_ARCHITECTURE.md` v2.1.20의 `NEXUS 공통 UI/UX 단일 계약`
- 적용 Manifest: `app-manifest.json` v1.3.8

## 목적

NEXUS 홈의 다크모드와 일반모드에서 반복되는 카드·버튼 외곽선을 줄이고, 미세한 면 명도 차이·간격·텍스트 위계로 앱과 상태를 읽게 한다. 일반모드는 순백색 대신 따뜻한 아이보리 종이 톤을 사용한다.

## 현재 상태와 목표 상태

- 현재 NEXUS 홈은 앱 카드·사용자 영역·버튼·상태 요소의 선과 면이 동시에 강조되어 화면이 잘게 분절돼 보인다.
- 목표는 다크모드의 차분한 흑연색과 일반모드의 아이보리 종이색을 한 화면모드 계약으로 제공하고, 상단에서 즉시 전환할 수 있게 하는 것이다.
- 기본 카드와 버튼은 면 차이와 텍스트로 구분하고, 키보드 포커스·오류·실제 입력 경계에는 필요한 선을 유지한다.
- 상단·하단의 구조 구분선은 유지한다. 문제 범위는 정보 텍스트를 압도하는 반복 카드·테이블 선이며, 홈에는 테이블이 없으므로 이번 단계에서는 앱 카드와 입력의 장식선을 대상으로 한다.

## 적용 경계

- 허용: `nexus/index.html`, 홈에만 적용되는 `nexus/nexus.css` 선택자, 홈의 공통 테마 컨트롤러 연동, 홈 캐시 버전과 관련 계약 테스트.
- 유지: 인쇄·Excel·이미지 출력, 앱 업무 로직, 인증·저장소·데이터 계약, 단축키, 화면 구조와 기존 동작.
- 금지: `nexus/common` 원본과 다른 업무 앱 변경, 관리자 화면 변경, 전역 테두리 삭제, 상·하단 구조선 삭제, 포커스 표시 제거, 상태 의미 변경.
- 검증: NEXUS 홈의 일반/다크 계산 스타일, 데스크톱·모바일 화면, 대비, 콘솔 오류와 홈 계약.

## 확인된 충돌과 처리

- 아키텍처 계약은 다크 패널·표·입력을 저채도 의미 계층으로 분리하고 키보드 포커스를 명확히 유지하도록 요구한다.
- 따라서 모든 선을 없애지 않고 홈의 기본·반복 경계만 낮추며, 상·하단 구조선과 포커스·오류 상태는 보존한다.
- 소스·Manifest·아키텍처 사이에 이번 시각 변경을 차단하는 충돌은 확인되지 않았다.

## 완료 증거

변경 파일, 계산 스타일, 대표 스크린샷, 테스트, commit·PR·CI·배포와 운영 확인을 이 문서 끝에 누적한다.

## 구현 결과

- `nexus/index.html`에 홈 전용 범위 클래스를 추가하고 CSS 캐시 버전을 `1.3.0`, 런타임 캐시 버전을 `1.3.1`로 올렸다.
- 홈의 사용자 구분 배지, 보조 버튼, 로그인 카드, 입력창과 앱 카드의 기본 보더를 제거했다. 상·하단 구조 구분선은 테마별 저대비 색상으로 유지했다.
- 배경·카드·입력의 낮은 명도 차이, 텍스트 크기와 간격으로 화면 위계를 구성했다.
- 강조색은 눈썹 문구와 주 행동에만 유지하고, 카드 화살표와 보조 동작은 낮은 대비의 장식으로 정리했다.
- 홈 앱 카드는 데스크톱 3열, 중간폭 2열, 모바일 1열로 구성하고 모바일 사용자 동작은 별도 행에 배치했다.
- 일반모드는 `#f3efe6` 아이보리 바탕과 따뜻한 종이색 면을 사용하고, 다크모드는 기존의 차분한 흑연색 면을 유지했다.
- 홈 상단에 공통 테마 초기화 계약을 연결하고 공통 헤더와 같은 `☼`·`☾` 아이콘, 스위치 역할과 라벨을 가진 화면모드 전환을 추가했다. 선택은 공통 저장 키를 통해 유지된다.
- 관리자 화면과 다른 업무 앱은 홈 전용 선택자 밖에 두어 변경하지 않았다.

## 검증 결과

- 데스크톱 로그인 화면과 로그인 후 12개 앱 카드 화면을 실제 브라우저에서 확인했다.
- 모바일 390×844에서 로고·사용자 동작·앱 카드의 비겹침과 1열 흐름을 확인했다.
- 아이보리·다크 로그인 화면을 데스크톱과 모바일 390×844의 실제 브라우저에서 확인했다.
- 기본 로그인 카드와 입력의 계산 border가 `0px`, 상·하단 구분선은 테마별 저대비 색상으로 표시됨을 확인했다.
- 화면모드 전환 뒤 `aria-checked`·접근성 라벨·로고가 함께 갱신되고, 새로고침 뒤 선택 모드가 유지됨을 확인했다.
- 아이보리 모드의 일반 텍스트 대비는 12.36:1, 가장 낮은 보조 텍스트 대비는 4.69:1이었다. 다크모드의 일반 텍스트 대비는 15.33:1, 가장 낮은 보조 텍스트 대비는 7.43:1이었다.
- 모바일 390px에서 가로 스크롤이 없음을 확인했다.
- 키보드 `:focus-visible` 공통 outline 규칙을 보존했다.
- 브라우저 console error는 0건이었다.
- `test-nexus-basic-login-home.mjs` 통과.
- `test-nexus-auth-min-control-ui.mjs` 통과.
- `test-nexus-company-profile.mjs` 47개 검사 통과.
- `validate-repository.mjs` 24개 검사, 경고 0건 통과.
- `git diff --check` 통과.
