import assert from'node:assert/strict';import{validateCompanyCloseScope}from'../orderq/dataops-close-core.js';const warehouses=[{warehouseId:'W1',revision:1,status:'ACTIVE'},{warehouseId:'W2',revision:2,status:'ACTIVE'},{warehouseId:'OLD',revision:3,status:'INACTIVE'}];const full=await validateCompanyCloseScope({companyId:'ONEAPP',warehouseIds:['W2','W1'],activeWarehouses:warehouses,warehouseMasterHeadDigest:'h',inventoryRows:[{productId:'P',warehouseId:'OLD',baseUnit:'EA',signedBaseQuantity:5},{productId:'Q',warehouseId:'UNASSIGNED',baseUnit:'EA',signedBaseQuantity:-1}],mode:'COMMIT'});assert.equal(full.official,true);assert.deepEqual(full.issues.map(row=>row.issueCode).sort(),['CLOSE_INACTIVE_WAREHOUSE_BALANCE','CLOSE_WAREHOUSE_UNASSIGNED']);const partial=await validateCompanyCloseScope({companyId:'ONEAPP',warehouseIds:['W1'],activeWarehouses:warehouses,warehouseMasterHeadDigest:'h',mode:'DRY_RUN'});assert.equal(partial.official,false);await assert.rejects(()=>validateCompanyCloseScope({companyId:'ONEAPP',warehouseIds:['W1'],activeWarehouses:warehouses,warehouseMasterHeadDigest:'h',mode:'COMMIT'}),/CLOSE_COMPANY_WAREHOUSE_SCOPE_REQUIRED/);console.log('PASS stage6 company scope');
