import assert from 'node:assert/strict';import{calculateInventoryClose,validateApprovedBaseline}from'../orderq/dataops-close-core.js';import{baseline,inventoryKey,movement}from'./dataops-stage6-test-fixtures.mjs';
validateApprovedBaseline(baseline);let result=calculateInventoryClose({businessDate:'2026-08-25',baselines:[baseline],movements:[movement('M1',5,1),movement('M2',-3,2)],dataOpsRows:[{...baseline,signedBaseQuantity:11,includedOrderQLedgerSequence:2}],bridgeUpperInclusive:2});assert.equal(result.rows[0].ledgerOpeningBase,-2);assert.equal(result.rows[0].ledgerCalculatedClosingBase,0);assert.equal(result.rows[0].dataOpsReportedClosingBase,11);assert.equal(result.rows[0].inventoryDifferenceBase,11);
result=calculateInventoryClose({businessDate:'2026-08-25',priorCloseBusinessDate:'2026-08-24',priorRows:[{inventoryKey,ledgerCalculatedClosingBase:10}],movements:[movement('M3',5,105,{businessDate:'2026-08-23'})],bridgeLowerExclusive:100,bridgeUpperInclusive:105});assert.equal(result.rows[0].ledgerCalculatedClosingBase,15);assert.equal(result.issues[0].issueCode,'BACKDATED_EFFECT_AFTER_CLOSE');assert.throws(()=>calculateInventoryClose({businessDate:'2026-08-25',movements:[],bridgeUpperInclusive:0}),/CLOSE_OPENING_BASE_REQUIRED/);console.log('PASS stage6 ledger opening bridge');
