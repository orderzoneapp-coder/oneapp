import assert from'node:assert/strict';import fs from'node:fs';const s=fs.readFileSync(new URL('../orderq/order-draft-editor.js',import.meta.url),'utf8');for(const n of['getDraft','setDraft','patchHeader','addLine','removeLine','focusLine','applyProduct','clearProduct','setPriceChoice','cyclePrice','validate','getDirtyFields','destroy'])assert.match(s,new RegExp(n));assert.doesNotMatch(s,/createOrder\(|updateOrder\(/);console.log('OrderDraftEditor PASS');
