import assert from'node:assert/strict';import fs from'node:fs';const e=fs.readFileSync(new URL('../orderq/intake-engine.js',import.meta.url),'utf8'),a=fs.readFileSync(new URL('../orderq/intake-document-adapter.js',import.meta.url),'utf8');for(const n of['captureTextIntake','analyzeSingleOrderDocument','saveExtractionReview','confirmExtraction','saveMatchingReview','confirmMatching','saveOrderCompletion','commitIntakeOrder','reopenIntakeStage'])assert.match(e,new RegExp(n));assert.match(a,/ORDERQ_INTAKE_MULTIPLE_DOCUMENTS_REQUIRES_STAGE4/);console.log('ORDER IN single document PASS');
