import assert from 'node:assert/strict';
import { readFileSync } from 'node:fs';
const smart=readFileSync(new URL('../smartinput/smartinput.js',import.meta.url),'utf8');
assert.match(smart,/DIRECT로 연결 해제/);
const ui=readFileSync(new URL('../orderq/purchase-ui.js',import.meta.url),'utf8');
for(const token of ['loadPurchaseStage3Capability','completePurchaseOfficial','ORDERQ_PURCHASE_STAGE3_CAPABILITY_UNAVAILABLE','purchaseSubmissions','PROJECTION_PENDING']) assert.ok(smart.includes(token),token);
for(const token of ['listOfficialPurchases','loadOfficialPurchaseAggregate','official-correct','official-reverse','LEGACY_PURCHASE_V1','PURCHASE_STAGE3_V1']) assert.ok(ui.includes(token),token);
assert.match(ui,/current\.document\.contractKind !== 'PURCHASE_STAGE3_V1'/);
assert.match(ui,/confirmPurchase\(confirmationCommand/);
console.log('ORDER Q stage3 purchase UI boundary tests passed');
