# MerchOps 카탈로그 비교형 행사가 작업화면 MVP 개발명세

- 작업 ID: `MO-20260730-01`
- 명세 버전: `1.0`
- 작성일: `2026-07-30`
- 대상 솔루션: `MerchOps`
- 대상 파일: `MerchOps.html`
- 개발 경로: `일반 개발`
- 목표 배포 버전: `v2.1.173_PromoCatalogCompareMVP`
- 현재 기준 버전: `v2.1.172_ExplicitMasterLookup`
- PM 검증: 1차 검증 필수, 문제가 있을 때만 수정·재검증 1회

---

## 1. 작업 목적

카탈로그, 새로 불러온 재고정보, 작업 시작 시점의 마스터 기준가격을 한 화면에서 비교하여 관리자가 이번 작업에 적용할 행사가·행사테마·판매여부를 직접 결정하도록 한다.

본 기능은 행사가를 자동으로 결정하는 기능이 아니다.

시스템은 다음 업무만 지원한다.

- 카탈로그와 불러온정보의 재고·LOT·원가 차이 표시
- 카탈로그와 마스터 스냅샷의 가격 차이 표시
- 검토 목적에 맞는 상품 필터링
- 관리자의 행사가·행사테마·판매여부 직접 입력
- 처리상태 및 작업값 출처 관리
- 완료 결과·작업이력·다음 카탈로그 저장

---

## 2. 핵심 업무 정의

### 2.1 비교는 날짜가 아니라 순서 기준이다

카탈로그와 불러온정보는 `어제/오늘` 또는 특정 날짜 기준으로 비교하지 않는다.

```text
카탈로그
= 먼저 존재하던 이전 기준 재고와 확정 작업정보

불러온정보
= 이번 작업을 위해 새로 불러온 재고 원본

이번 작업 확정 결과
= 다음 작업에서 사용할 카탈로그
```

업무 흐름은 1기와 2기를 비교하는 방식과 같다.

```text
카탈로그(이전 기준)
+ 불러온정보(새 기준)
+ 작업 시작 시점 마스터 스냅샷
→ 관리자가 비교·검토
→ 이번 작업값 확정
→ 다음 카탈로그 생성
```

알고리즘에 기준일, 마감일, 날짜 선후관계 또는 경과시간을 적용하지 않는다.

### 2.2 재고 차이는 판매수량이 아니다

카탈로그 재고와 불러온 재고의 차이는 재고 상태 변화만 의미한다.

```text
카탈로그 재고 8
불러온 재고 3
→ 카탈로그 대비 재고 5 감소
```

다음과 같이 해석하거나 표시하지 않는다.

- 5개 판매
- 행사가로 5개 판매
- 행사 잔여수량 3
- 초과판매
- 판매수량 소진
- 소진 즉시 가격 전환

재고 차이에는 판매 외에 입고·이동·조정·폐기·손실·누락 등이 포함될 수 있다.

### 2.3 LOT는 행사가 적용단위가 아니다

행사가는 품목코드 기준 작업값이다.

LOT는 다음 판단을 돕는 참고정보로만 사용한다.

- 기존 재고와 새로 불러온 재고 구성
- 구매처
- 구매단가
- 원가 변동
- 카탈로그 LOT의 현재 잔존 여부
- 새 LOT 발생 여부

특정 LOT에만 서로 다른 판매가를 적용하거나 LOT별 행사수량을 차감하지 않는다.

### 2.4 최종 판단은 관리자에게 있다

시스템은 다음 작업을 자동 실행하지 않는다.

- 행사가 생성·수정·삭제
- 행사 종료
- 시중가 또는 출고가 자동 전환
- 새 LOT의 행사 자동 포함
- 상품 삭제
- 판매중지 또는 판매재개

필터·이슈·리포트는 검토 후보와 근거만 제공한다.

---

## 3. 이번 명세에서 폐기하는 기존 표현과 기능

기존 초안 또는 회의 중 제시됐더라도 다음 내용은 적용하지 않는다.

### 3.1 날짜·비교범위 메타정보

두 스냅샷에 다음 값을 저장하거나 비교조건으로 사용하지 않는다.

- 기준일
- 마감시각
- 마감 완료 여부
- 대상 창고 비교검증
- 재고 포함 기준 비교검증
- 0재고 포함 여부 비교검증
- 품목코드 정규화 기준 버전 비교검증
- 단위 환산 기준 버전 비교검증
- LOT 구분 기준 버전 비교검증
- 파일 스키마 버전 비교검증
- 원본 파일 식별값 비교검증

위 값의 불일치를 이유로 비교·편집·완료를 차단하는 기능을 만들지 않는다.

### 3.2 판매수량 및 행사수량 관리

다음 기능은 개발범위에서 제외한다.

- DataOps 판매자료 연결
- 주문별 행사판매 차감
- 실시간 행사 적용수량
- 행사 판매수량
- 행사 잔여수량
- 초과판매 판단
- 취소·반품에 따른 행사수량 복원

### 3.3 금지 표현

화면·이슈·리포트에서 다음 표현을 사용하지 않는다.

- 전일 재고 / 오늘 재고
- 행사 설정수량
- 행사 판매수량
- 행사 잔여수량
- 정확히 N개 판매
- 초과판매
- 판매수량 소진
- 행사 LOT 소진
- 소진 즉시 시중가 전환

권장 표현은 다음과 같다.

- 카탈로그 재고
- 불러온 재고
- 카탈로그 대비 재고 증가·감소
- 동일 LOT 현재 잔존
- 새 LOT
- 카탈로그 LOT 미잔존
- 행사 종료 검토

화면에서 업무상 `오늘 작업값`이라는 문구를 사용하는 경우에도 날짜 계산의 의미를 부여하지 않는다. 이는 이번에 적용할 작업값을 뜻한다.

---

## 4. 데이터 영역

기능 내부 데이터는 다음 네 영역으로 분리한다.

| 영역 | 의미 | 편집 |
|---|---|---|
| 마스터 스냅샷 | 작업 시작 시점의 마스터 기준가격과 상태 | 읽기 전용 |
| 카탈로그 | 이전 작업에서 저장된 비교 기준 | 읽기 전용 |
| 불러온 원본 | 이번에 불러온 재고·LOT·원가 정보 | 읽기 전용 |
| 오늘 작업값 | 이번 작업에서 적용할 행사가·테마·판매상태 | 편집 가능 |

코드 내부에서는 기존 마스터 필드 `카탈로그`와 혼동되지 않도록 역할이 드러나는 별도 이름을 사용한다.

권장 내부 이름:

```text
masterSnapshot
previousCatalog
loadedRaw
workDraft
```

기존 `sources.catalog` 또는 마스터의 `카탈로그` 필드 의미를 임의로 변경하지 않는다.

---

## 5. 입력자료

### 5.1 카탈로그 파일

MVP에서는 카탈로그를 파일로 불러온다.

카탈로그는 다음 작업을 위한 이전 비교 기준이다.

필수 논리정보:

- 품목코드
- 품목명
- 규격·단위
- 재고
- LOT 구성
- 구매처
- 구매단가
- 저장 당시 마스터 기준가격
- 확정 출고가
- 확정 행사가
- 행사테마
- 판매여부
- 작업상태
- 확정 여부
- 작업값 출처
- 관리자 완료 여부

### 5.2 불러온 재고정보

불러온정보는 이번 작업의 재고 원본이다.

표시·비교 정보:

- 품목코드
- 품목명
- 규격·단위
- 재고
- 구매처
- 구매단가
- 창고
- LOT 구성

DataOps 수불마감 파일을 입력으로 사용하는 경우:

- 총재고는 `전체재고`를 기준으로 한다.
- LOT·구매처·구매단가 근거는 `구매잔량`과 `기타상품`을 참고한다.
- `전체재고`, `구매잔량`, `기타상품`의 수량을 서로 더하지 않는다.
- `수불마감_분석원장`은 원가·수불 근거 확인용이며 총재고에 중복 합산하지 않는다.

### 5.3 마스터 스냅샷

작업화면을 구성할 때 현재 마스터값을 복사해 고정한다.

고정 대상:

- 입고가
- 출고가
- 시중가
- 행사가
- 행사테마
- 현재 적용 판매가
- 판매여부

작업 중 다른 화면이나 다른 PC에서 마스터가 바뀌어도 현재 비교결과를 자동 변경하지 않는다.

`최신 마스터 불러오기`를 실행할 때:

1. 현재 스냅샷과 최신 마스터 차이를 먼저 보여준다.
2. 관리자가 적용을 확정해야 스냅샷을 교체한다.
3. 직접 수정한 작업값은 덮어쓰지 않는다.
4. 관련 마스터값이 바뀐 완료행은 작업값을 보존하고 `확인 중`으로 전환한다.
5. 이슈에 `마스터 변경 후 재확인`을 표시한다.

마스터 조회일시나 날짜는 비교 알고리즘에 사용하지 않는다.

---

## 6. 상품 및 LOT 매칭

### 6.1 상품목록

화면에는 다음 합집합을 표시한다.

```text
전체 상품
= 마스터 스냅샷 상품
∪ 카탈로그 상품
∪ 불러온정보 상품
```

한쪽에만 있는 상품도 삭제하지 않고 행을 유지한다.

### 6.2 상품 매칭

상품은 정규화된 품목코드를 기본키로 매칭한다.

- 고유 품목코드 1건: 자동 매칭
- 카탈로그에만 존재: 불러온 영역에 `오늘 정보 없음`
- 불러온정보에만 존재: 카탈로그 영역에 `카탈로그 없음`
- 품목코드 공란: `미매칭`
- 동일 영역 내 품목코드 중복: 자동 보존·자동 병합 금지, `미매칭`
- 품목코드가 변경돼 대응관계가 불명확: 자동 추정 금지, `미매칭`

품목명·규격 유사도를 이용해 임의로 자동 연결하지 않는다.

### 6.3 LOT 매칭

LOT 매칭은 날짜를 사용하지 않는다.

우선순위:

1. 원본에 존재하는 고유 LOT 번호 또는 입고번호
2. 품목코드 + 구매처 + 구매단가 + 창고 + 단위
3. 위 조합이 동일한 경우 하나의 비교용 재고그룹으로 합산

동일 조건의 LOT가 여러 개라 개별 대응이 불가능하면 개별 LOT를 임의 연결하지 않고 그룹 비교로 표시한다.

LOT 상태 표현:

- 동일 LOT 현재 잔존
- 새 LOT
- 카탈로그 LOT 미잔존
- LOT 미매칭

`소진`이라는 확정 표현은 사용하지 않는다.

---

## 7. 화면 구조

기존 MerchOps 작업화면의 기본 구조를 유지한다.

```text
마스터 / 이슈 / 비교정보 / 리포트
```

### 7.1 진입 및 작업버튼

기존 `카탈로그` 버튼은 마스터 작업범위 선택 기능이므로 의미를 변경하지 않는다.

새 기능은 행사작업 영역의 별도 `행사가 비교` 버튼으로 진입한다.

비교모드 상단 작업버튼:

```text
[카탈로그 불러오기] [재고 불러오기] [최신 마스터] [비교결과 저장]
```

- `카탈로그 불러오기`: 이전 비교 기준 파일을 불러온다.
- `재고 불러오기`: 이번 비교에 사용할 재고 원본을 불러온다.
- `최신 마스터`: 차이 미리보기 후 관리자 확인을 받아 마스터 스냅샷을 교체한다.
- `비교결과 저장`: 적용결과·작업이력·다음카탈로그 시트를 가진 XLSX를 생성한다.

행별 또는 선택행 일괄 작업으로 `완료`, `보류`, `확인 중으로 변경`을 제공한다.

비교모드에 진입하는 시점의 마스터값을 `마스터 스냅샷`으로 고정한다. 마스터가 비어 있으면 비교모드 진입을 중단하고 기존 마스터 불러오기 안내를 표시한다.

`비교정보`는 같은 열 안에서 카탈로그값과 불러온값을 위아래로 비교한다.

기본 비교항목:

```text
재고 / 입고가 / 출고가 / 행사가 / 행사테마 / 판매여부
```

LOT 상세는 기본 행을 펼쳐 높이를 변경하지 않고 우측 고정 상세패널로 제공한다.

권장 상세패널 폭은 데스크톱 기준 380~420px이며, 기본 테이블의 행 높이와 열 정렬을 유지해야 한다.

---

## 8. 비교 셀 표시 규칙

### 8.1 고정 높이

- 모든 상품 행과 비교 셀은 두 줄 기준 고정 높이를 사용한다.
- 값이 같아 한 줄만 표시돼도 셀 높이를 줄이지 않는다.
- 긴 값은 행 높이를 늘리지 않고 말줄임 처리한다.
- 모든 상품에서 열과 행의 수직 정렬을 유지한다.

### 8.2 같은 값

불러온값 하나만 셀 가운데에 표시한다.

```text
24,000
```

셀 높이는 두 줄 기준을 그대로 유지한다.

### 8.3 다른 값

위쪽에 카탈로그값을 작고 연하게, 아래쪽에 불러온값을 크게 표시한다.

```text
20,000
23,000
```

- 변경된 셀은 아주 옅은 배경색만 적용한다.
- 화살표, 차액, 굵은 테두리, 이슈 배지를 셀 안에 중복 표시하지 않는다.
- 차이 수치는 이슈 영역에서만 표시한다.

### 8.4 0·공란·행 없음·미매칭

다음 상태를 반드시 구분한다.

| 상태 | 화면 표시 |
|---|---|
| 실제 숫자 0 | `0` |
| 상품행은 있으나 해당 값이 공란 | `—` |
| 카탈로그 상품행 없음 | `카탈로그 없음` |
| 불러온 상품행 없음 | `오늘 정보 없음` |
| 중복 코드·변경 코드 등 연결 불가 | `미매칭` |

예:

```text
카탈로그 8 → 불러온값 0
8
0

카탈로그 없음 → 불러온값 5
카탈로그 없음
5

카탈로그 8 → 불러온 상품행 없음
8
오늘 정보 없음
```

공란을 숫자 0으로 바꾸지 않는다.

### 8.5 편집값

- 마스터 스냅샷: 읽기 전용
- 카탈로그값: 읽기 전용
- 불러온 원본값: 읽기 전용
- 오늘 작업값: 입력창에서만 편집

카탈로그는 비교 기준이고 입력창은 이번 작업에 적용할 값이다.

관리자 입력이 카탈로그 또는 불러온 원본을 덮어쓰면 안 된다.

---

## 9. 작업값 초기화와 출처

### 9.1 초기화 우선순위

1. 카탈로그 행이 `완료`이고 확정 여부와 관리자 완료 여부가 모두 참이면 확정 작업값을 승계한다.
2. 카탈로그 행이 미처리·확인 중·보류이면 해당 값을 비교 참고로만 표시하고 확정값으로 자동 승계하지 않는다.
3. 확정 승계값이 없으면 현재 MerchOps의 기존 마스터 기본값 초기화 규칙을 사용한다.
4. 새 상품의 행사가를 자동 생성하지 않는다.

### 9.2 작업값 출처

각 작업 필드의 출처를 저장한다.

- `승계`: 완료·확정된 카탈로그값
- `마스터`: 마스터 기본값
- `직접수정`: 관리자가 기존값을 변경
- `신규입력`: 관리자가 공란에 새 값을 입력
- `삭제`: 관리자가 기존 행사가를 명시적으로 제거

행사가·테마·판매여부를 관리자가 수정하면 해당 필드 출처를 즉시 갱신한다.

### 9.3 행사가 작업동작

내부 작업상태는 다음을 구분한다.

- `KEEP`: 기존 확정 행사가 유지
- `SET`: 행사가 신규입력 또는 수정
- `CLEAR`: 관리자가 행사가 삭제

`CLEAR`는 관리자의 명시적인 삭제 작업이다.

행사가 삭제 후 실제 적용 판매가 계산은 기존 MerchOps 가격 우선순위를 그대로 사용한다. 이번 개발에서 출고가·시중가 우선순위를 새로 변경하지 않는다.

행사테마는 행사가와 별도 필드로 관리한다.

- 행사가를 삭제해도 행사테마를 자동 삭제하지 않는다.
- 행사가가 없고 행사테마가 남으면 `행사가는 없고 테마가 남아 있음` 이슈를 표시한다.
- 행사테마 삭제 여부는 관리자가 결정한다.

---

## 10. 재불러오기

같은 종류의 파일을 다시 불러오면 기존 원본에 추가 병합하지 않고 해당 원본영역을 교체한다.

### 10.1 교체 절차

1. 새 파일 분석
2. 교체 전 미리보기 표시
3. 유지·신규·사라짐·중복·미매칭 건수 표시
4. 관리자 교체 확인
5. 해당 원본영역 교체
6. 상품·LOT 재매칭
7. 이슈·필터·리포트 재계산

### 10.2 작업값 보존

- 고유 품목코드가 유지된 상품: 기존 오늘 작업값 보존
- 새로 생긴 상품: 작업값 초기화 규칙 적용
- 새 파일에서 사라진 상품: 기존 작업값을 삭제하지 않고 `오늘 정보 없음`·`보류` 처리
- 중복 또는 변경된 품목코드: 작업값 자동 보존 금지, `미매칭`·`확인 중` 처리
- 관리자가 직접 수정한 값: 자동 덮어쓰기 금지

불러온 원본 또는 LOT·원가가 달라진 완료행은 작업값을 보존하되 `확인 중`으로 전환하고 `원본 변경 후 재확인` 이슈를 표시한다.

카탈로그와 불러온정보의 불러오기 순서가 달라도 최종 매칭결과가 같아야 한다.

---

## 11. 목적 필터

최종 목적 필터:

```text
[전체] [기존행사] [행사종료] [가격변동] [신규입고] [기타검토]
```

각 필터에는 상품 수를 표시한다.

한 상품은 여러 목적 필터에 동시에 포함될 수 있다.

### 11.1 기존행사

완료·확정된 카탈로그에 행사가가 존재하는 상품.

### 11.2 행사종료

카탈로그에 행사가가 있고 다음 중 하나에 해당하는 검토 후보.

- 불러온 재고가 숫자 0
- 불러온 상품행 없음
- 카탈로그에서 확인되던 LOT가 불러온정보에 모두 미잔존
- 카탈로그 LOT는 미잔존이고 새 LOT만 존재

이 필터는 `행사 종료 검토` 후보만 조회한다.

필터 진입 또는 조건 충족만으로 행사가·테마·판매여부를 변경하지 않는다.

### 11.3 가격변동

다음 중 하나가 달라진 상품.

- 마스터 스냅샷 기준시세
- 마스터 스냅샷 출고가
- 카탈로그 대비 구매원가
- 현재 적용 판매가 참고값

공란과 숫자 0은 서로 다른 값으로 판정한다.

### 11.4 신규입고

- 카탈로그에는 없고 불러온정보에만 있는 상품
- 기존 상품에 새 LOT가 존재하는 상품

행사가를 자동 생성하지 않는다.

### 11.5 기타검토

주요 목적 필터에 한 번도 포함되지 않은 상품.

```text
기타검토
= 전체
- (기존행사 ∪ 행사종료 ∪ 가격변동 ∪ 신규입고)
```

### 11.6 전체

현재 비교화면의 모든 상품.

---

## 12. 처리상태 필터

목적 필터와 처리상태 필터를 분리하며 교차 적용할 수 있어야 한다.

```text
상태: 전체 / 미처리 / 확인 중 / 완료 / 보류
```

- 미처리: 관리자가 아직 작업을 시작하지 않음
- 확인 중: 값을 입력했거나 원본·마스터 변경으로 재확인이 필요함
- 완료: 관리자가 이번 작업값을 확정함
- 보류: 추가 확인이 필요해 확정하지 않음

예:

```text
기존행사 + 미처리
행사종료 + 확인 중
신규입고 + 보류
가격변동 + 완료
```

필터 변경만으로 원본값·작업값·처리상태를 변경하지 않는다.

---

## 13. 이슈 표시

이슈는 사실과 변화량을 짧게 표시한다.

권장 표현:

- 기준시세 상승·하락
- 출고가 상승·하락
- 구매원가 상승·하락
- 재고 증가·감소
- 동일 LOT 현재 잔존
- 새 LOT
- 카탈로그 LOT 미잔존
- 카탈로그 없음
- 오늘 정보 없음
- 마스터 미등록
- 미매칭
- 행사 종료 검토
- 원본 변경 후 재확인
- 마스터 변경 후 재확인
- 행사가 없이 테마만 존재

예:

```text
재고 -5
구매원가 +3,000
```

모호한 `가격상승`, `가격하락` 단독 표현은 사용하지 않는다.

행사테마는 실제 코드 기준으로 표시한다.

```text
테마 1
테마 1,3
```

필요한 경우 상세패널에서 코드명을 함께 표시한다.

---

## 14. 리포트

MVP 리포트는 AI 해석이 아니라 규칙형 문장 조합으로 생성한다.

기본 행에는 중요한 내용 1~2줄만 표시한다.

예:

```text
재고 8→3, 구매원가 3,000원 상승
카탈로그 LOT 2개 중 1개 잔존, 새 LOT 1개
```

리포트는 다음 원칙을 지킨다.

- 재고 감소 원인을 판매로 단정하지 않음
- 행사가 유지·수정·삭제를 자동 권고하지 않음
- 사실과 검토 필요성을 구분
- 상세 근거는 우측 LOT 상세패널에서 제공

---

## 15. 관리자 작업 흐름

```text
1. 작업범위 선택
2. 행사작업의 [행사가 비교] 진입
3. 진입 시점 마스터 스냅샷 고정
4. 카탈로그 파일 불러오기
5. 재고정보 파일 불러오기
6. 기존행사 확인
7. 행사종료 후보 확인
8. 가격변동 확인
9. 신규입고 확인
10. 기타검토로 누락 확인
11. 행사가·행사테마·판매여부 직접 입력
12. 행별 완료 또는 보류 처리
13. [비교결과 저장]으로 완료 결과·작업이력·다음 카탈로그 저장
14. 완료행만 기존 MerchOps 적용절차로 반영
```

---

## 16. 저장 및 적용

### 16.1 MVP 공식 저장물

MVP의 공식 이관수단은 브라우저 저장소가 아니라 Excel 파일이다.

한 개의 XLSX에 다음 논리 시트를 생성한다.

1. `적용결과`
2. `작업이력`
3. `다음카탈로그`

브라우저 저장소를 사용하더라도 현재 PC의 임시 작업복구 용도로만 사용하며 다른 PC에서 이어지는 공식 저장수단으로 안내하지 않는다.

### 16.2 적용결과

`완료` 상태이며 관리자 완료 여부가 참인 상품만 포함한다.

포함값:

- 품목코드
- 확정 출고가
- 확정 행사가
- 행사테마
- 판매여부
- 작업값 출처
- 관리자 완료 여부

미처리·확인 중·보류·미매칭 상품은 포함하지 않는다.

### 16.3 작업이력

다음 상품을 포함한다.

- 값이 수정된 상품
- 완료 상품
- 보류 상품
- 원본 또는 마스터 변경으로 재확인된 상품

포함값:

- 품목코드
- 필드
- 변경 전 값
- 변경 후 값
- 작업값 출처
- 처리상태
- 관리자 완료 여부

작업이력 시트는 작업과정 기록이다.

기존 공유 마스터 히스토리에는 실제 F7 적용으로 마스터가 변경된 결과만 기존 방식으로 기록한다. 보류·미처리 작업을 마스터 변경이력으로 기록하지 않는다.

### 16.4 다음카탈로그

불러온 원본정보 전체와 작업상태를 저장한다.

완료 상품뿐 아니라 미처리·확인 중·보류·미매칭 상품도 원본 보존 목적으로 포함한다.

필수값:

- 불러온 재고 원본
- LOT 구성
- 구매처·구매단가
- 작업 출고가
- 작업 행사가
- 행사테마
- 판매여부
- 작업상태
- 확정 여부
- 작업값 출처
- 관리자 완료 여부

다음 작업에서 자동 승계할 수 있는 값은 `완료 + 확정 + 관리자 완료`를 모두 충족한 상품뿐이다.

미처리·확인 중·보류·미매칭 값은 참고자료로만 보존하며 확정값으로 자동 승계하지 않는다.

### 16.5 F7·F8·F9 유지조건

- 비교모드의 F7 마스터 반영대상은 완료·확정된 상품만 허용한다.
- 기존 마스터 저장의 검증·히스토리·원자적 복구 절차를 우회하지 않는다.
- F8의 기존 출력 의미를 임의로 변경하지 않는다. 비교형 저장물이 F8에 연결될 경우 기존 출력과 충돌하지 않는 별도 명시 동작으로 구현한다.
- F9 Export Center 이동 의미를 변경하지 않는다.
- 견적·구매·재고·정보 등 기존 모드의 F7·F8·F9 동작은 유지한다.

---

## 17. 변경범위

### 17.1 포함

- MerchOps 내 카탈로그 비교형 행사가 작업모드
- 카탈로그 파일 불러오기
- 불러온 재고정보와 위아래 비교
- 작업 시작 시점 마스터 스냅샷
- 상품 합집합 생성
- 품목코드 및 LOT 비교
- 고정 높이 비교 셀
- 0·공란·행 없음·미매칭 구분
- 목적 필터와 처리상태 필터
- 이슈 및 규칙형 리포트
- 우측 LOT 상세패널
- 작업값 직접 편집
- 재불러오기 교체 및 직접수정값 보존
- 완료·보류 상태관리
- 적용결과·작업이력·다음카탈로그 XLSX 저장
- 완료행만 마스터 반영
- 배포 버전 3곳 일치 표시

### 17.2 제외

- DataOps 판매정산 연결
- 주문별 판매수량 차감
- 실시간 행사수량
- 자동 행사가 결정
- 자동 행사종료
- 자동 가격 전환
- 자동 행사테마 삭제
- LOT별 판매가
- 날짜 기준 비교
- 비교범위 메타정보 검증
- 공통 저장키 변경
- `coreEngine.js` 공통 계약 변경
- `code.gs` 클라우드 API 변경
- DataOps 생산 로직 변경
- 기존 마스터 필드 구조 변경
- 복잡한 사용자 정의 규칙 편집기

---

## 18. 기존 기능 유지사항

다음 기존 기능과 의미를 변경하지 않는다.

- 마스터 상품 조회
- 파서리스트와 기존 마스터 `카탈로그` 필드
- 카탈로그 작업범위 선택
- 견적 비교
- 구매 불러오기
- 재고 불러오기
- 정보 불러오기
- 정지관리
- 행사작업의 기존 직접수정 기능
- F7 마스터 반영의 검증·복구·히스토리
- F8 기존 Excel 출력
- F9 Export Center 이동
- SmartParser 즉시 마스터 반영
- DataOps 수불·원가·재고 계산
- 공통 저장키 및 클라우드 동기화 계약

기존 `카탈로그`라는 명칭이 파서리스트·작업범위 의미로 사용되고 있으므로 새 비교 카탈로그 데이터와 내부 상태를 반드시 분리한다.

---

## 19. 오류 및 예외처리

### 19.1 파일 오류

- 지원하지 않는 파일형식: 불러오기 중단
- 필수 헤더 없음: 해당 영역 불러오기 중단
- 품목코드 공란: 행 보존 후 `미매칭`
- 품목코드 중복: 자동 병합 금지, `미매칭`
- 파싱 실패: 기존 작업상태 유지, 교체하지 않음
- 정상적인 0행 파일: 오류와 구분해 안내하며 기존 데이터를 자동 삭제하지 않음

### 19.2 저장 오류

- XLSX 생성 실패: 기존 작업상태 유지
- 다운로드 실패: 완료로 표시하지 않음
- 마스터 반영 실패: 기존 원자적 복구절차로 이전 마스터·히스토리 복원
- 일부 행만 저장되는 부분 성공 금지

### 19.3 관리자 판단이 필요한 경우

- 품목코드 중복
- 품목코드 변경 추정
- LOT 미매칭
- 카탈로그만 있고 불러온정보 없음
- 행사가 없이 테마만 존재
- 완료 후 원본 또는 마스터 변경

위 경우 값을 자동 확정하지 않는다.

---

## 20. 테스트 요구사항

### 20.1 전용 자동검사

다음 전용 회귀검사를 추가한다.

1. 카탈로그와 불러온정보의 불러오기 순서가 달라도 결과 동일
2. 상품 합집합 유지
3. 고유 품목코드 매칭
4. 중복·공란·변경 코드 미매칭
5. 날짜를 사용하지 않는 LOT 매칭
6. 숫자 0, 공란, 카탈로그 없음, 오늘 정보 없음, 미매칭 구분
7. 같은 값 한 줄 표시와 두 줄 고정 높이
8. 다른 값 위·아래 표시
9. 셀 내부 차액·화살표·배지 미출력
10. 목적 필터 중복 허용
11. 기타검토가 주요 필터 합집합의 여집합인지 확인
12. 필터만으로 작업값이 변경되지 않음
13. 행사가 종료 후보가 자동으로 행사가를 삭제하지 않음
14. 재불러오기 시 원본영역 교체
15. 고유 코드 작업값 보존
16. 사라진 상품 작업값 보존 및 보류
17. 직접수정값 미덮어쓰기
18. 원본·마스터 변경 시 완료행을 확인 중으로 전환
19. 완료·확정행만 적용결과 포함
20. 미처리·확인 중·보류 값 자동승계 금지
21. 다음카탈로그에 전체 원본 및 상태 보존
22. 행사가와 행사테마 독립 처리
23. 공란을 0으로 변환하지 않음
24. DataOps 총재고와 LOT 근거 수량 중복합산 금지

### 20.2 XLSX 재열기 검사

생성된 XLSX를 다시 열어 다음을 확인한다.

- `적용결과`, `작업이력`, `다음카탈로그` 시트 존재
- 헤더 순서와 자료형 유지
- 숫자 0이 숫자 0으로 유지
- 공란이 공란으로 유지
- 미처리·보류 상품이 적용결과에 없음
- 미처리·보류 상품이 다음카탈로그에는 존재
- 완료·확정 여부와 작업값 출처 보존
- 수식 오류 없음

### 20.3 기존 회귀검사

- MerchOps 마스터 로딩
- 견적·구매·재고·정보 불러오기
- 기존 카탈로그 작업범위
- F7 마스터 반영·히스토리·복구
- F8 기존 출력
- F9 Export Center
- 정지관리
- 행사 초기화 및 직접수정
- SmartParser·DataOps·공통 저장 계약검사

### 20.4 운영 검증

비식별 합성자료로 다음 조합을 검증한다.

- 양쪽 동일 상품
- 카탈로그에만 있는 상품
- 불러온정보에만 있는 상품
- 재고 8→0
- 공란→0
- 카탈로그 LOT 미잔존
- 새 LOT 추가
- 품목코드 중복
- 직접수정 후 재불러오기
- 완료 후 원본 변경
- 완료·보류·미처리 혼합 저장

실제 운영 웹에서 파일 불러오기부터 XLSX 다운로드·재열기까지 확인한다.

---

## 21. 완료조건

1. 카탈로그·불러온정보·마스터 스냅샷을 한 화면에서 비교할 수 있다.
2. 비교 알고리즘이 날짜를 사용하지 않는다.
3. 카탈로그와 불러온정보의 값 위치가 동일하다.
4. 모든 상품 행은 두 줄 기준 고정 높이를 유지한다.
5. 같은 값은 하나만 표시하고 다른 값은 위·아래로 표시한다.
6. 숫자 0·공란·카탈로그 없음·오늘 정보 없음·미매칭이 구분된다.
7. 카탈로그와 불러온 원본은 편집할 수 없다.
8. 오늘 작업값만 입력창에서 편집할 수 있다.
9. 관리자가 행사가·테마·판매여부를 직접 결정한다.
10. 상품과 LOT의 불확실한 매칭을 자동 확정하지 않는다.
11. 재불러오기 시 해당 원본영역만 교체된다.
12. 고유 코드의 직접수정값이 보존된다.
13. 사라진 상품의 작업값이 삭제되지 않고 보류된다.
14. 완료 후 원본·마스터 변경 시 재확인 상태로 전환된다.
15. 목적 필터가 중복 포함을 허용한다.
16. 기타검토가 주요 목적 필터에 포함되지 않은 상품을 보여준다.
17. 필터·이슈·리포트가 작업값을 자동 변경하지 않는다.
18. 완료·확정행만 적용결과와 마스터 반영대상이 된다.
19. 미처리·확인 중·보류·미매칭 값이 다음 작업의 확정값으로 자동 승계되지 않는다.
20. 생성 XLSX의 세 논리 시트가 재열기 검사에서 정상이다.
21. 기존 MerchOps 업무모드와 공통 저장 계약에 회귀가 없다.
22. 운영 웹에서 목표 버전과 실제 기능을 확인할 수 있다.

---

## 22. 배포 버전 표시

배포 시 다음 세 위치의 버전을 동일하게 표시한다.

- HTML 문서 제목
- 초기 로딩 또는 시작 화면
- 실제 화면 상단 버전 배너

확정 목표 버전:

```text
v2.1.173_PromoCatalogCompareMVP
```

구현 중 main의 MerchOps 버전이 먼저 올라간 경우 개발자는 최신 버전 다음 번호로 조정하고 세 위치를 동일하게 맞춘 뒤 기획자에게 확정 버전을 보고한다.

---

## 23. 개발·검증·배포 절차

### 23.1 개발 담당

1. 최신 main과 현재 버전 확인
2. 별도 작업 브랜치 생성
3. 기존 카탈로그 작업범위와 새 비교 카탈로그 상태 분리
4. 기능 구현
5. 전용 자동검사 및 기존 회귀검사
6. 실제 XLSX 생성·재열기
7. Draft PR 생성
8. 기획자 대화방에 개발 결과 전달
9. 상태를 `PM 검증 대기`로 종료

### 23.2 PM 검증

중점 검증:

- 날짜·판매수량 개념이 다시 들어가지 않았는지
- 자동 행사가 변경이 없는지
- 0·공란·행 없음이 보존되는지
- 재불러오기에서 직접수정값이 보호되는지
- 완료행만 적용되는지
- 기존 F7 원자적 저장·복구가 유지되는지
- 기존 MerchOps 모드와 다른 솔루션에 영향이 없는지

PM은 검증 결과를 기획자 대화방에 전달하고 `기획자 최종 보고 대기`로 종료한다.

### 23.3 병합·배포

본 작업은 일반 개발이므로 PM 1차 검증과 병합 권고 후 병합·배포한다.

배포 후 개발 담당자는 다음을 확인한다.

- 운영 URL 신규 버전
- 세 버전 표기 일치
- 실제 카탈로그·재고 파일 불러오기
- 작업값 편집
- 필터 및 상태관리
- XLSX 다운로드·재열기
- 완료행만 적용대상인지

최종 사용자 완료 보고는 기획 담당자만 수행한다.

---

## 24. 영향범위 및 롤백

### 예상 영향

- 직접 영향: `MerchOps.html`
- 검증 영향: MerchOps 전용 테스트, 저장소 CI
- 간접 확인: 공통 마스터 저장, 히스토리, Export Center
- 변경하지 않음: DataOps, SmartParser, `coreEngine.js`, `code.gs`, 공통 저장키

### 롤백

- 기능을 추가한 병합 커밋을 revert한다.
- 새 공통 저장키나 데이터 마이그레이션을 만들지 않으므로 별도 데이터 복구는 없어야 한다.
- 이미 F7으로 반영된 마스터 변경은 기존 마스터 히스토리 및 복구정책을 따른다.
- 롤백 후 기존 견적·구매·재고·정보 업무 흐름을 운영환경에서 재확인한다.

---

## 25. 최종 개발 정의

```text
카탈로그
↕
불러온 재고·LOT·원가
↕
작업 시작 시점 마스터 기준가격
→ 차이와 검토 후보 표시
→ 관리자가 행사가·테마·판매여부 직접 결정
→ 완료 결과·작업이력·다음 카탈로그 저장
```

본 기능은 두 순서의 재고 상태와 가격 근거를 비교하는 관리자 작업화면이다.

재고 차이를 판매량으로 해석하지 않고, 날짜를 비교 알고리즘에 사용하지 않으며, 시스템이 행사가나 행사종료를 자동 결정하지 않는다.
