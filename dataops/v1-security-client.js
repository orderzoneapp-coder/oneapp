(function(global){
  'use strict';
  const EXPECTED_DEPLOYMENT=Object.freeze({deploymentId:'',deploymentVersion:'',gitCommit:''});
  const CAPABILITY='DATAOPS_SNAPSHOT_V1_SECURITY_V1',CUTOVER='SERVER_FIRST_V1';
  const text=value=>String(value??'').trim();
  const exactDeployment=(value,expected=EXPECTED_DEPLOYMENT)=>['deploymentId','deploymentVersion','gitCommit'].every(key=>text(expected[key])&&text(value?.[key])===text(expected[key]));
  function evaluate(ping,expected=EXPECTED_DEPLOYMENT){return Boolean(exactDeployment(ping,expected)&&ping?.capabilityVersion===CAPABILITY&&ping?.mode===CUTOVER&&ping?.readAuthRequired===true&&ping?.writeAuthRequired===true&&JSON.stringify(ping?.roles)===JSON.stringify(['DATAOPS_SNAPSHOT_V1_READ','DATAOPS_SNAPSHOT_V1_WRITE'])&&JSON.stringify(ping?.actions)===JSON.stringify(['dataops_snapshot_get','dataops_snapshot_commit']));}
  function createClient({expectedDeployment=EXPECTED_DEPLOYMENT,fetchImpl=global.fetch}={}){let connection=null;const requireCredential=value=>{const credential={token:text(value?.token),actorId:text(value?.actorId),deviceId:text(value?.deviceId),environment:text(value?.environment),scope:{companyId:text(value?.scope?.companyId)}};if(!credential.token||!credential.actorId||!credential.scope.companyId)throw new Error('DATAOPS_V1_ACCESS_DENIED');return Object.freeze(credential);};return Object.freeze({released:()=>['deploymentId','deploymentVersion','gitCommit'].every(key=>text(expectedDeployment[key])),ready:()=>Boolean(connection),clear:()=>{connection=null;},async connect({url,readCredential,writeCredential}){if(!['deploymentId','deploymentVersion','gitCommit'].every(key=>text(expectedDeployment[key])))throw new Error('DATAOPS_V1_SECURITY_NOT_RELEASED');const read=requireCredential(readCredential),write=requireCredential(writeCredential);if(read.actorId!==write.actorId||JSON.stringify(read.scope)!==JSON.stringify(write.scope))throw new Error('DATAOPS_V1_SCOPE_MISMATCH');const response=await fetchImpl(text(url),{method:'POST',headers:{'Content-Type':'text/plain;charset=utf-8'},body:JSON.stringify({action:'dataops_v1_security_ping'})}),json=await response.json();if(!response.ok||json?.status!=='success'||json?.action!=='dataops_v1_security_ping'||!evaluate(json.data,expectedDeployment))throw new Error('DATAOPS_V1_SECURITY_CAPABILITY_REQUIRED');connection={url:text(url),read,write};return true;},envelope(operation){if(!connection)throw new Error('DATAOPS_V1_SECURITY_CONNECTION_REQUIRED');const credential=operation==='WRITE'?connection.write:connection.read;return{token:credential.token,actorId:credential.actorId,deviceId:credential.deviceId,environment:credential.environment,scope:credential.scope};},async commitSnapshot({legacyModule,productData,targetDateStr,url,readCredential,writeCredential}){if(!this.released())return legacyModule.commit({productData,targetDateStr,url});if(!connection)await this.connect({url,readCredential,writeCredential});const snapshot=await legacyModule.buildSnapshot({productData,targetDateStr}),response=await fetchImpl(text(url),{method:'POST',headers:{'Content-Type':'text/plain;charset=utf-8'},body:JSON.stringify({action:'dataops_snapshot_commit',snapshot,...this.envelope('WRITE')})}),json=await response.json();if(!response.ok||json?.status!=='success'||json?.action!=='dataops_snapshot_commit'||!json.data)throw new Error('DATAOPS_V1_WRITE_FAILED');return{snapshot,saved:json.data};}});}
  global.DATAOPS_V1_SECURITY_CLIENT=Object.freeze({EXPECTED_DEPLOYMENT,evaluate,createClient,client:createClient()});
})(typeof window!=='undefined'?window:globalThis);
