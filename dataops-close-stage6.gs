/** DataOps Close V1 staging authority. Not enabled until immutable deployment identity is released. */
const DATAOPS_CLOSE_V1='DATAOPS_CLOSE_V1';
const DATAOPS_CLOSE_PROPERTIES=Object.freeze({DEPLOYMENT_ID:'ONEAPP_DATAOPS_CLOSE_DEPLOYMENT_ID',DEPLOYMENT_VERSION:'ONEAPP_DATAOPS_CLOSE_DEPLOYMENT_VERSION',GIT_COMMIT:'ONEAPP_DATAOPS_CLOSE_GIT_COMMIT',POINTERS:'ONEAPP_DATAOPS_CLOSE_POINTERS_JSON'});
const DATAOPS_CLOSE_SHEETS=Object.freeze({SEALS:'DataOpsClose_Seals',LEASES:'DataOpsClose_Leases',CHUNKS:'DataOpsClose_Chunks',INDEX:'DataOpsClose_Index',AUDIT:'DataOpsClose_Audit'});
const DATAOPS_CLOSE_ACTIONS=Object.freeze(['dataops_close_ping','dataops_close_seal','dataops_close_prepare','dataops_close_write_chunks','dataops_close_commit','dataops_close_abort']);
const DATAOPS_V1_SECURITY=Object.freeze({MODE_PROPERTY:'ONEAPP_DATAOPS_V1_SECURITY_MODE',LEGACY:'LEGACY_COMPATIBLE',CUTOVER:'SERVER_FIRST_V1',CAPABILITY:'DATAOPS_SNAPSHOT_V1_SECURITY_V1',READ_ROLE:'DATAOPS_SNAPSHOT_V1_READ',WRITE_ROLE:'DATAOPS_SNAPSHOT_V1_WRITE'});
function dataOpsCloseText(value){return String(value===undefined||value===null?'':value).trim();}
function dataOpsV1SecurityMode(properties){return dataOpsCloseText(properties.getProperty(DATAOPS_V1_SECURITY.MODE_PROPERTY))===DATAOPS_V1_SECURITY.CUTOVER?DATAOPS_V1_SECURITY.CUTOVER:DATAOPS_V1_SECURITY.LEGACY;}
function dataOpsV1SecurityCapability(properties){const mode=dataOpsV1SecurityMode(properties);return{capabilityVersion:DATAOPS_V1_SECURITY.CAPABILITY,mode,readAuthRequired:mode===DATAOPS_V1_SECURITY.CUTOVER,writeAuthRequired:mode===DATAOPS_V1_SECURITY.CUTOVER,roles:[DATAOPS_V1_SECURITY.READ_ROLE,DATAOPS_V1_SECURITY.WRITE_ROLE]};}
function dataOpsV1RequireAccess(payload,operation,properties){if(dataOpsV1SecurityMode(properties)!==DATAOPS_V1_SECURITY.CUTOVER)return{legacyCompatible:true};const role=operation==='WRITE'?DATAOPS_V1_SECURITY.WRITE_ROLE:DATAOPS_V1_SECURITY.READ_ROLE;return dataOpsSituationRequireAuth(payload,role,properties);}
function dataOpsV1PreflightAction(action,payload,properties){if(action==='dataops_snapshot_commit')return dataOpsV1RequireAccess(payload,'WRITE',properties);if(action==='dataops_snapshot_get')return dataOpsV1RequireAccess(payload,'READ',properties);return null;}
function dataOpsCloseDeployment(properties){const result={deploymentId:dataOpsCloseText(properties.getProperty(DATAOPS_CLOSE_PROPERTIES.DEPLOYMENT_ID)),deploymentVersion:dataOpsCloseText(properties.getProperty(DATAOPS_CLOSE_PROPERTIES.DEPLOYMENT_VERSION)),gitCommit:dataOpsCloseText(properties.getProperty(DATAOPS_CLOSE_PROPERTIES.GIT_COMMIT))};if(!result.deploymentId||!result.deploymentVersion||!result.gitCommit)throw new Error('DATAOPS_CLOSE_DEPLOYMENT_NOT_RELEASED');return result;}
function dataOpsCloseSheet(ss,name,headers){let sheet=ss.getSheetByName(name);if(!sheet)sheet=ss.insertSheet(name);if(sheet.getLastRow()===0)sheet.appendRow(headers);return sheet;}
function dataOpsCloseRows(sheet){if(sheet.getLastRow()<2)return[];const values=sheet.getRange(2,1,sheet.getLastRow()-1,sheet.getLastColumn()).getValues(),headers=sheet.getRange(1,1,1,sheet.getLastColumn()).getValues()[0];return values.map((row,index)=>headers.reduce((out,key,column)=>{out[key]=row[column];return out;},{_row:index+2}));}
function dataOpsCloseAudit(ss,row){dataOpsCloseSheet(ss,DATAOPS_CLOSE_SHEETS.AUDIT,['auditId','action','actorId','roleIds','scopeDigest','seriesId','commandId','result','detail','createdAt']).appendRow([Utilities.getUuid(),row.action,row.actorId,(row.roleIds||[]).join(','),row.scopeDigest,row.seriesId||'',row.commandId||'',row.result||'SUCCESS',JSON.stringify(row.detail||{}),new Date().toISOString()]);}
function dataOpsCloseLeaseSheet(ss){return dataOpsCloseSheet(ss,DATAOPS_CLOSE_SHEETS.LEASES,['seriesId','commandId','idempotencyKey','fingerprint','status','stageId','expiresAt','updatedAt','sealedVerification','actionType','expectedSeriesHeadRevision','targetRevision']);}
function dataOpsCloseChunkSheet(ss){return dataOpsCloseSheet(ss,DATAOPS_CLOSE_SHEETS.CHUNKS,['stageId','kind','chunkIndex','chunkDigest','content','status','updatedAt']);}
function dataOpsCloseIndexSheet(ss){return dataOpsCloseSheet(ss,DATAOPS_CLOSE_SHEETS.INDEX,['closeRevisionId','seriesId','revision','actionType','sourceADigest','resultBDigest','issueDecisionDigest','finalReceiptFingerprint','receipt','createdAt']);}
function dataOpsCloseSealSheet(ss){return dataOpsCloseSheet(ss,DATAOPS_CLOSE_SHEETS.SEALS,['sourceSealId','closeSeriesId','sourceADigest','closeReadTokenDigest','orderqHeadDigestAtSeal','dataopsHeadDigestAtSeal','orderqDeployment','dataopsDeployment','capabilityDigest','sealedBy','sealedAt','receiptFingerprint','receipt']);}
function dataOpsCloseFindLease(ss,seriesId){return dataOpsCloseRows(dataOpsCloseLeaseSheet(ss)).find(row=>dataOpsCloseText(row.seriesId)===seriesId);}
function dataOpsCloseVerifyReadSources(ss,payload,auth,properties){const orderRequest=payload.orderqReadRequest||{},orderSession=orderQM9SituationReadCached('SESSION',orderRequest.readSessionId);orderQM9SituationRequireIdentity(orderSession,orderRequest);if(dataOpsCloseText(orderRequest.tokenDigest)!==dataOpsCloseText(orderSession.tokenDigest)||Date.now()>=Date.parse(orderSession.expiresAt))throw new Error('CLOSE_READ_TOKEN_EXPIRED');const stored=dataOpsSituationReadSessions(ss).rows.find(row=>row.readSessionId===orderSession.dataOpsReadSessionId),dataSession=stored&&stored.payload,recalculated=dataSession&&dataOpsSituationSessionToken(dataSession,properties),dataToken=dataOpsCloseText(payload.dataopsReadTokenDigest);if(!dataSession||stored.status!=='OPEN'||!dataOpsSituationConstantTime(dataSession.tokenDigest,recalculated)||!dataOpsSituationConstantTime(dataSession.tokenDigest,dataToken)||Date.now()>=Date.parse(dataSession.expiresAt)||auth.actorId!==dataSession.actorId||auth.scopeDigest!==dataSession.scopeDigest)throw new Error('CLOSE_READ_TOKEN_EXPIRED');const dataDeployment=dataOpsSituationRequireDeployment(properties);if(dataDeployment.deploymentId!==dataSession.deploymentId||dataDeployment.deploymentVersion!==dataSession.deploymentVersion||dataDeployment.gitCommit!==dataSession.gitCommit)throw new Error('CLOSE_DEPLOYMENT_CHANGED_AFTER_SEAL');const tokenCore={orderqReadSessionId:orderSession.readSessionId,orderqTokenDigest:orderSession.tokenDigest,dataopsReadSessionId:dataSession.readSessionId,dataopsTokenDigest:dataSession.tokenDigest,crossAuthorityHandshakeDigest:orderSession.crossAuthorityHandshakeDigest,expiresAt:new Date(Math.min(Date.parse(orderSession.expiresAt),Date.parse(dataSession.expiresAt))).toISOString()};return{tokenDigest:dataOpsSituationDigest(tokenCore),orderq:orderSession,dataops:dataSession,expiresAt:tokenCore.expiresAt};}
function dataOpsCloseSeal(ss,payload,auth){const properties=PropertiesService.getScriptProperties(),verified=dataOpsCloseVerifyReadSources(ss,payload,auth,properties),snapshot=payload.closeSnapshotA||{},sourceADigest=dataOpsSituationDigest(snapshot);if(sourceADigest!==dataOpsCloseText(payload.sourceADigest)||!dataOpsCloseText(payload.closeSeriesId))throw new Error('CLOSE_SOURCE_DIGEST_INVALID');const receipt={sourceSealId:'CSS-'+Utilities.getUuid(),closeSeriesId:dataOpsCloseText(payload.closeSeriesId),sourceADigest,closeReadTokenDigest:verified.tokenDigest,orderqHeadDigestAtSeal:dataOpsCloseText(verified.orderq.headDigest),dataopsHeadDigestAtSeal:dataOpsCloseText(verified.dataops.headDigest),orderqDeployment:{deploymentId:verified.orderq.deploymentId,deploymentVersion:verified.orderq.deploymentVersion,gitCommit:verified.orderq.gitCommit},dataopsDeployment:{deploymentId:verified.dataops.deploymentId,deploymentVersion:verified.dataops.deploymentVersion,gitCommit:verified.dataops.gitCommit},capabilityDigest:dataOpsCloseText(payload.capabilityDigest),sealedBy:auth.actorId,sealedAt:new Date().toISOString()};if(!receipt.orderqHeadDigestAtSeal||!receipt.dataopsHeadDigestAtSeal||!receipt.capabilityDigest)throw new Error('CLOSE_SOURCE_SEAL_INVALID');receipt.receiptFingerprint=dataOpsSituationDigest(receipt);dataOpsCloseSealSheet(ss).appendRow([receipt.sourceSealId,receipt.closeSeriesId,receipt.sourceADigest,receipt.closeReadTokenDigest,receipt.orderqHeadDigestAtSeal,receipt.dataopsHeadDigestAtSeal,JSON.stringify(receipt.orderqDeployment),JSON.stringify(receipt.dataopsDeployment),receipt.capabilityDigest,receipt.sealedBy,receipt.sealedAt,receipt.receiptFingerprint,JSON.stringify(receipt)]);dataOpsCloseAudit(ss,{action:'SEAL',actorId:auth.actorId,roleIds:auth.roleIds,scopeDigest:auth.scopeDigest,seriesId:receipt.closeSeriesId,detail:{sourceSealId:receipt.sourceSealId}});return receipt;}
function dataOpsClosePrepare(ss,payload,auth){const intent=payload.intent||{},seriesId=dataOpsCloseText(intent.closeSeriesId),commandId=dataOpsCloseText(intent.commandId),idempotencyKey=dataOpsCloseText(intent.idempotencyKey),fingerprint=dataOpsCloseText(intent.fingerprint),stageId=dataOpsCloseText(intent.stageId),sealed=intent.sealedVerification||{},actionType=dataOpsCloseText(intent.actionType);if(!seriesId||!commandId||!idempotencyKey||!/^[a-f0-9]{64}$/.test(fingerprint)||!stageId||!['POST_CLOSE','CORRECT_CLOSE','REVERSE_CLOSE'].includes(actionType))throw new Error('DATAOPS_CLOSE_INTENT_INVALID');for(const key of ['orderqHeadDigest','dataopsHeadDigest','capabilityDigest','issueDecisionDigest','resultBDigest'])if(!dataOpsCloseText(sealed[key]))throw new Error('DATAOPS_CLOSE_INTENT_INVALID');const sheet=dataOpsCloseLeaseSheet(ss),prior=dataOpsCloseFindLease(ss,seriesId);if(prior&&prior.status==='PREPARED'&&Date.parse(prior.expiresAt)>Date.now()&&prior.commandId!==commandId)throw new Error('DATAOPS_CLOSE_LEASE_CONFLICT');if(prior&&prior.idempotencyKey===idempotencyKey&&prior.fingerprint!==fingerprint)throw new Error('DATAOPS_CLOSE_IDEMPOTENCY_CONFLICT');const expiresAt=new Date(Date.now()+120000).toISOString(),now=new Date().toISOString(),row=[seriesId,commandId,idempotencyKey,fingerprint,'PREPARED',stageId,expiresAt,now,JSON.stringify(sealed),actionType,Number(intent.expectedSeriesHeadRevision||0),Number(intent.targetRevision||0)];if(prior)sheet.getRange(prior._row,1,1,row.length).setValues([row]);else sheet.appendRow(row);dataOpsCloseAudit(ss,{action:'PREPARE',actorId:auth.actorId,roleIds:auth.roleIds,scopeDigest:auth.scopeDigest,seriesId,commandId});return{seriesId,stageId,status:'PREPARED',expiresAt};}
function dataOpsCloseWriteChunks(ss,payload,auth){const stageId=dataOpsCloseText(payload.stageId),kind=dataOpsCloseText(payload.kind),chunks=Array.isArray(payload.chunks)?payload.chunks:[];if(!stageId||!['A','B','ISSUES','AUDIT','REPORT'].includes(kind)||chunks.length>512)throw new Error('DATAOPS_CLOSE_CHUNK_INVALID');const sheet=dataOpsCloseChunkSheet(ss),rows=dataOpsCloseRows(sheet),stageRows=rows.filter(row=>row.stageId===stageId),projected=new Map(stageRows.map(row=>[row.kind+'|'+Number(row.chunkIndex),String(row.content||'')]));chunks.forEach(chunk=>{const content=String(chunk.content||''),index=Number(chunk.chunkIndex),digest=dataOpsCloseText(chunk.chunkDigest);if(!Number.isInteger(index)||index<0||content.length>45000||dataOpsSituationUtf8Bytes(content)>45000||dataOpsSituationDigest(content)!==digest)throw new Error('DATAOPS_CLOSE_CHUNK_INVALID');const prior=rows.find(row=>row.stageId===stageId&&row.kind===kind&&Number(row.chunkIndex)===index);if(prior&&prior.chunkDigest!==digest)throw new Error('DATAOPS_CLOSE_CHUNK_CONFLICT');projected.set(kind+'|'+index,content);});const totalBytes=[...projected.values()].reduce((sum,content)=>sum+dataOpsSituationUtf8Bytes(content),0);if(projected.size>512||totalBytes>20*1024*1024)throw new Error('DATAOPS_CLOSE_PAYLOAD_TOO_LARGE');chunks.forEach(chunk=>{const index=Number(chunk.chunkIndex),prior=rows.find(row=>row.stageId===stageId&&row.kind===kind&&Number(row.chunkIndex)===index);if(!prior)sheet.appendRow([stageId,kind,index,chunk.chunkDigest,String(chunk.content||''),'VERIFIED',new Date().toISOString()]);});dataOpsCloseAudit(ss,{action:'WRITE_CHUNKS',actorId:auth.actorId,roleIds:auth.roleIds,scopeDigest:auth.scopeDigest,detail:{stageId,kind,count:chunks.length,totalCount:projected.size,totalBytes}});return{stageId,kind,verifiedCount:chunks.length,totalCount:projected.size,totalBytes};}
function dataOpsCloseCommit(ss,payload,auth,properties){const intent=payload.intent||{},seriesId=dataOpsCloseText(intent.closeSeriesId),lease=dataOpsCloseFindLease(ss,seriesId);if(!lease||lease.status!=='PREPARED'||Date.parse(lease.expiresAt)<=Date.now()||lease.commandId!==dataOpsCloseText(intent.commandId)||lease.fingerprint!==dataOpsCloseText(intent.fingerprint)||lease.actionType!==dataOpsCloseText(intent.actionType))throw new Error('DATAOPS_CLOSE_LEASE_INVALID');const fresh=payload.freshVerification||{},sealed=JSON.parse(lease.sealedVerification||'{}');for(const key of ['orderqHeadDigest','dataopsHeadDigest','capabilityDigest','issueDecisionDigest','resultBDigest'])if(!dataOpsCloseText(fresh[key])||dataOpsCloseText(fresh[key])!==dataOpsCloseText(sealed[key]))throw new Error('CLOSE_SOURCE_CHANGED_AFTER_SEAL');if(Number(intent.expectedSeriesHeadRevision||0)!==Number(lease.expectedSeriesHeadRevision||0)||Number(intent.targetRevision||0)!==Number(lease.targetRevision||0))throw new Error('CLOSE_REVISION_CONFLICT');const indexSheet=dataOpsCloseIndexSheet(ss),indexes=dataOpsCloseRows(indexSheet),same=indexes.find(row=>row.finalReceiptFingerprint===intent.finalReceiptFingerprint);if(same)return JSON.parse(same.receipt);const seriesRows=indexes.filter(row=>row.seriesId===seriesId);if(seriesRows.length!==Number(intent.expectedSeriesHeadRevision||0))throw new Error('CLOSE_REVISION_CONFLICT');const chunks=dataOpsCloseRows(dataOpsCloseChunkSheet(ss)).filter(row=>row.stageId===lease.stageId);for(const kind of ['A','B','ISSUES'])if(!chunks.some(row=>row.kind===kind&&row.status==='VERIFIED'))throw new Error('CLOSE_STAGE_READBACK_REQUIRED');const receipt={closeRevisionId:intent.closeRevisionId,closeSeriesId:seriesId,revision:Number(intent.revision),finalReceiptFingerprint:intent.finalReceiptFingerprint,status:'COMMITTED',committedAt:new Date().toISOString()};indexSheet.appendRow([receipt.closeRevisionId,seriesId,receipt.revision,intent.actionType,intent.sourceADigest,intent.resultBDigest,intent.issueDecisionDigest,intent.finalReceiptFingerprint,JSON.stringify(receipt),receipt.committedAt]);const readBack=dataOpsCloseRows(indexSheet).find(row=>row.closeRevisionId===receipt.closeRevisionId&&row.finalReceiptFingerprint===receipt.finalReceiptFingerprint);if(!readBack)throw new Error('CLOSE_INDEX_READBACK_REQUIRED');const pointers=JSON.parse(properties.getProperty(DATAOPS_CLOSE_PROPERTIES.POINTERS)||'{}');pointers[seriesId]={seriesHeadRevisionId:receipt.closeRevisionId,currentEffectiveRevisionId:intent.currentEffectiveRevisionId||receipt.closeRevisionId};properties.setProperty(DATAOPS_CLOSE_PROPERTIES.POINTERS,JSON.stringify(pointers));const leaseSheet=dataOpsCloseLeaseSheet(ss);leaseSheet.getRange(lease._row,5).setValue('COMMITTED');dataOpsCloseAudit(ss,{action:'COMMIT',actorId:auth.actorId,roleIds:auth.roleIds,scopeDigest:auth.scopeDigest,seriesId,commandId:lease.commandId,detail:{revision:receipt.revision}});return receipt;}
function dataOpsCloseHandleAction(ss,action,payload){const properties=PropertiesService.getScriptProperties(),deployment=dataOpsCloseDeployment(properties);if(action==='dataops_close_ping')return{contractVersion:DATAOPS_CLOSE_V1,capabilityVersion:DATAOPS_CLOSE_V1,...deployment,actions:DATAOPS_CLOSE_ACTIONS};const actionType=dataOpsCloseText(payload&&payload.intent&&payload.intent.actionType),role=action==='dataops_close_commit'?(actionType==='CORRECT_CLOSE'?'DATAOPS_CLOSE_CORRECT':actionType==='REVERSE_CLOSE'?'DATAOPS_CLOSE_REVERSE':'DATAOPS_CLOSE_COMMIT'):action==='dataops_close_prepare'||action==='dataops_close_write_chunks'||action==='dataops_close_abort'?'DATAOPS_CLOSE_RECONCILE':'DATAOPS_CLOSE_READ',auth=dataOpsSituationRequireAuth(payload,role,properties);try{if(action==='dataops_close_prepare')return dataOpsClosePrepare(ss,payload,auth);if(action==='dataops_close_seal')return dataOpsCloseSeal(ss,payload,auth);if(action==='dataops_close_write_chunks')return dataOpsCloseWriteChunks(ss,payload,auth);if(action==='dataops_close_commit')return dataOpsCloseCommit(ss,payload,auth,properties);if(action==='dataops_close_abort'){const lease=dataOpsCloseFindLease(ss,dataOpsCloseText(payload.closeSeriesId));if(lease){dataOpsCloseLeaseSheet(ss).getRange(lease._row,5).setValue('ABORTED');}return{status:'ABORTED'};}throw new Error('DATAOPS_CLOSE_ACTION_UNSUPPORTED');}catch(error){dataOpsCloseAudit(ss,{action,actorId:auth.actorId,roleIds:auth.roleIds,scopeDigest:auth.scopeDigest,result:'FAILURE',detail:{code:dataOpsCloseText(error&&error.message)}});throw error;}}
