# AGENTS.md

## 공통 작업 규칙
- 인증을 반복하지 않는다.
- 기존 저장소가 있으면 다시 clone하지 않는다.
- 동일 실패를 반복하지 않는다.
- 하나의 명령이 30초 이상 진행되면 중단하고, 현재 상태·지연 원인·다음 선택지를 보고한다.
- main 브랜치를 직접 수정하지 않는다.
- 작업 범위 밖의 변경은 하지 않는다.

## 승인 정책
- 일반적인 수정·테스트·커밋·push는 연속해서 진행할 수 있다.
- 삭제, merge, 배포, 데이터 구조 변경, 복구가 어려운 작업만 실행 전에 보고한다.

## OneApp 저장소 규칙
- MerchOps와 DataOps는 목적과 운영이 분리된 솔루션이다.
- 두 솔루션은 상품, 단가, 재고, 매출 등 자료를 연동한다.
- 공통 데이터 필드나 Core 변경 시 연동 영향을 먼저 검토한다.
- 특정 솔루션 작업 중 관련 없는 다른 솔루션 기능을 임의 변경하지 않는다.
