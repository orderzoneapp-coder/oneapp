# AGENTS.md

Version: 1.4.0
Last Updated: 2026-07-31

이 문서는 OneApp 저장소에서 작업하는 Codex 및 AI 개발 도구의 공통 작업 규칙이다.  
저장소 루트의 본 문서를 작업 시작 전에 확인하고 적용한다.

---

## 1. 기본 작업 원칙

- 요청된 작업 범위만 수정한다.
- 관련 없는 기능이나 파일은 임의로 변경하지 않는다.
- 기존 기능, 데이터 형식, 파일 구조를 우선 보존한다.
- 필요한 정보가 부족하거나 목적이 불명확하면 추측하지 말고 확인한다.
- 최소 수정, 최소 영향, 기존 호환성을 우선한다.
- 사용자가 요청하지 않은 기능 확장이나 리팩터링을 하지 않는다.
- 원본 엑셀값, 공란, 숫자 0 등 운영 데이터는 임의로 보정하거나 대체하지 않는다.
- 프로그램이 계산한 결과를 무조건 정답으로 간주하지 않는다.
- 관리자가 원본 자료와 처리 결과를 직접 비교할 수 있어야 한다.
- 최종 운영 정책과 업무 판단은 관리자가 결정한다.
- 기술적 안전성은 개발 담당자와 PM 검증 담당자가 확인한다.
- 관리자는 코드 검토나 기술 구조 승인을 담당하지 않는다.

---

## 2. 역할 구분

OneApp 개발 협업은 다음 역할로 구분한다.

### 2.1 기획 담당

기획 담당자는 사용자와 업무 목적을 협의하고 개발 가능한 명세를 작성한다.

주요 역할:

- 사용자의 실제 의도와 현재 문제 확인
- 관련 소스코드와 기존 기능 확인
- 확정사항, 검토사항, 추정사항 구분
- 개발 난이도와 진행 경로 결정
- 변경 범위, 처리 규칙, 유지사항, 완료조건 작성
- 담당 개발 프로젝트 지정
- 개발 담당자에게 실행 가능한 개발 명령 전달
- 개발·검증·배포 결과를 취합해 사용자에게 최종 보고

기획 담당자는 코드를 직접 수정하거나 commit, push, merge, 배포하지 않는다.

### 2.2 개발 담당

개발 담당자는 기획 명세에 따라 실제 코드를 수정하고 테스트한다.

주요 역할:

- 관련 소스코드와 기존 처리 흐름 확인
- 코드 작성·수정
- 로컬 및 자동 테스트
- 실제 파일 또는 안전한 복사본 검증
- commit, push, Pull Request 등록
- PM 피드백 반영
- 승인된 절차에 따른 병합·자동 배포
- 운영환경 정상 작동 확인
- 기획 담당자에게 결과 전달

개발 담당자는 사용자에게 전체 협업 과정의 최종 완료를 선언하지 않는다.

### 2.3 PM 검증 담당

PM 검증 담당자는 개발 결과와 Pull Request를 독립적으로 검증한다.

주요 역할:

- 기획 내용과 실제 변경 내용 일치 여부 확인
- 요청하지 않은 변경이나 기능 확장 여부 확인
- 기존 기능과 연관 솔루션 영향 확인
- CI와 테스트 결과 확인
- 데이터 손실·누락·중복·저장 실패 위험 확인
- 병합 가능 여부 판단
- 개발 담당자에게 수정 피드백 전달
- 수정 후 재검증
- 기획 담당자에게 최종 검증 결과 전달

PM은 코드를 직접 수정하거나 commit, push, merge, Ready 전환, 배포하지 않는다.

### 2.4 관리자

관리자는 다음을 담당한다.

- 업무 목적과 운영 정책 결정
- 정책 변경 여부 판단
- 배포 후 실제 화면과 업무 결과 확인
- 최종 운영 완료 판단

관리자에게 코드 안전성이나 기술 구조 승인을 요청하지 않는다.

---

## 3. 사용자 협의 및 이해 확인

기획 담당자는 개발명세를 바로 확정하지 않고 사용자와 업무 목적을 먼저 협의한다.

- 어려운 개발 용어를 그대로 사용하지 않는다.
- 개발 용어가 필요한 경우 실제 화면과 업무 흐름을 기준으로 쉽게 설명한다.
- 사용자가 이해하기 어려운 기술 선택을 승인 대상으로 제시하지 않는다.
- 사용자가 말한 내용을 짧게 다시 정리해 이해가 맞는지 확인한다.
- 선택지가 있는 경우 실제 운영 결과가 어떻게 달라지는지 설명한다.
- 장점뿐 아니라 기존 기능 영향과 위험도 함께 설명한다.
- 모호한 요청은 임의로 해석하지 않는다.
- 중요한 판단은 한 번에 여러 개 묻지 않고 순서대로 확인한다.
- 사용자가 단순히 “오케이”, “진행”, “그렇게 해”라고 답했더라도 충분히 이해했다고 확인되지 않으면 정책 확정으로 처리하지 않는다.

개발명세 확정 전 다음 내용을 확인한다.

- 현재 무엇이 불편한가
- 어떤 결과를 원하는가
- 반드시 유지해야 할 기존 기능은 무엇인가
- 자동 처리와 관리자 판단의 경계는 무엇인가
- 오류나 예외 발생 시 어떻게 처리할 것인가
- 개발 완료 후 어떤 화면과 결과를 확인할 것인가

필요한 경우 다음 형식으로 사용자에게 최종 확인한다.

```text
[기획 이해 확인]

현재 문제:
원하는 동작:
유지할 기존 기능:
변경하지 않을 범위:
예외 처리:
완료 후 확인할 결과:
```

---

## 4. 규칙 우선순위와 단일 원본

규칙은 다음 순서로 적용한다.

1. 사용자가 확정한 업무정책과 개발명세
2. 공통 `AGENTS.md`
3. `APP_ARCHITECTURE.md`
4. 담당별 지침
5. 작업방의 기술 실행계획

- `AGENTS.md`는 공통 행동과 개발·검증·Git·병합·배포 절차의 단일 원본이다.
- `APP_ARCHITECTURE.md`는 앱 방향, 구조와 공유계약의 기준이다.
- 담당별 지침은 역할, 권한과 소통 경계만 정의하며 공통 절차는 이 문서를 참조한다.
- 같은 공통 규칙을 역할 문서에 다시 상세히 적어 서로 다른 원본을 만들지 않는다.
- 역할 규칙과 구조 규칙이 충돌하면 어느 한쪽을 임의로 무시하지 않고 담당 경로로 확인한다.
- 작업방 제목과 지정 역할에 따라 기획자는 `roles/PLANNER.md`, PM은 `roles/PM.md`, 개발자는 `roles/DEVELOPER.md`를 함께 적용한다.
- 역할이 지정되지 않았거나 작업방 제목의 역할과 실제 요청이 다르면 작업을 시작하지 않고 담당 경로로 확인한다.

---

## 5. 개발 분류와 업무 흐름

### 5.1 빠른 처리

- 사용자가 명시적으로 빠른 처리로 지정한 제한적 작업에만 적용한다.
- 기획자가 사용자와 범위, 방식과 완료 조건을 확정해 담당 개발자에게 전달한다.
- PM은 개입하지 않는다.
- 개발자는 확정 범위의 구현·테스트와 결과 보고를 수행한다.
- 병합·배포는 승인 범위에 포함된 경우에만 수행한다.
- 공통 Core, 데이터 구조, 인증·권한, 운영 저장소, 다른 솔루션 또는 복구가 어려운 영향이 발견되면 즉시 중단하고 기획자에게 보고한다.

### 5.2 일반 개발

- 기존 기능 영향이나 업무규칙 변경 가능성이 있는 작업이다.
- `기획자 → PM → 개발자 → PM → 기획자 → 사용자` 경로로 진행한다.
- PM은 명세 일치, 범위 이탈, 직접 테스트와 치명적 위험을 중심으로 최소 검증한다.
- 문제가 있을 때만 개발자 수정과 필요한 범위의 재검증을 진행한다.

### 5.3 중요 개발

- 저장·삭제·마이그레이션, Master·공통 저장구조, 여러 앱 공유 데이터, 인증·권한·승인·감사 이력, 대량 데이터 계산 또는 복구 위험이 있는 작업이다.
- 일반 개발과 같은 전달 경로를 사용한다.
- PM은 실제 사고 가능성이 있는 핵심 위험을 정밀 검증한다.
- 재검증 후에도 중대한 문제가 남으면 반복하지 않고 기획자와 사용자에게 판단을 요청한다.

### 5.4 일반 개발·중요 개발 공통 흐름

1. 기획자가 사용자와 업무정책, 처리규칙, 예외, 유지 기능과 완료 조건을 확정한다.
2. 기획자가 개발명세와 개발 분류를 PM에게 전달한다.
3. PM이 정확한 프로젝트, 담당 개발자, 공통 작업 ID와 개발 작업방을 지정한다.
4. PM이 명세, 허용·금지 범위, 완료 조건, 검증 기준, 승인 범위와 중단 조건을 개발자에게 전달한다.
5. 개발자는 지정 범위만 구현·테스트하고 PM에게 결과를 보고한다.
6. PM은 진행 상태와 범위를 통제하고 필요하면 중단·보존·재사용·재작성 여부를 판단한다.
7. PM은 결과를 위험 기반으로 검증하고 필요한 수정사항을 개발자에게 전달한다.
8. PM은 병합·배포 경로를 관리하고 결과를 확인한다.
9. PM은 최종 결과와 남은 위험을 기획자에게 보고한다.
10. 기획자는 PM 보고를 기준으로 사용자에게 최종 보고한다.

---

## 6. 작업 ID, 작업방과 승인 정책

### 6.1 작업 ID와 작업방

- 하나의 업무에는 하나의 공통 작업 ID만 사용한다.
- 기획, 개발과 PM 검증은 같은 작업 ID와 표준 작업명을 공유한다.
- 작업방 제목은 `[기획|개발|검증][프로젝트][작업 ID] 작업명` 형식을 사용한다.
- 다른 솔루션 작업, 별도 배포가 필요한 작업, 기존 명세 밖 추가 개선 또는 독립적으로 완료되는 후속 작업만 별도 작업 ID를 사용할 수 있다.
- 일반 개발과 중요 개발의 새 개발 작업방과 PR별 PM 검증 작업방은 PM만 개설하거나 개설하도록 지시한다.
- 빠른 처리의 개발 작업방은 기획자가 개설한다.
- 개발자는 임의로 새 작업 ID나 작업방을 만들지 않는다.
- 같은 작업 ID와 동일 PR의 수정·재검증은 기존 작업방에서 이어간다.
- `ONEAPP 기획` 프로젝트(`fa769387-0958-4ab4-a6c6-a1e449a7f981`)는 사용자가 직접 개설하거나 명시적으로 개설을 지시한 기획 작업방에만 사용한다.
- 일반 개발과 중요 개발의 PM 작업방은 `ONEAPP PM 검증` 프로젝트(`2b3098e7-366b-4b31-928c-7910cf918cbf`)에 생성한다.
- 개발 작업방은 실제 대상 솔루션 프로젝트에 생성한다. 현재 등록 프로젝트는 `DataOps`(`23c9e51a-746a-4ca3-84eb-c3f0b6f54e41`), `MerchOps`(`81846edd-d097-4ab1-b78e-1eea44e37242`), `ORDERZ 쇼핑몰`(`d96d2c68-3a3e-4802-800e-a0ed851eb3bc`)이다.
- 대상 솔루션 프로젝트나 `projectId`를 확정할 수 없으면 `ONEAPP 기획` 프로젝트로 대체 생성하지 않고 기존 기획 작업방에 프로젝트 등록 필요 사유를 보고한다.
- 작업 생성 직후 PM은 작업 ID, 실제 `projectId`, 프로젝트명, 저장소, 작업 경로, 기준 SHA와 담당 역할을 다시 확인한다.
- 지정 프로젝트와 다르거나 `projectId`가 확인되지 않으면 작업지시를 보내거나 개발을 시작하지 않는다.
- 잘못 생성되었거나 작업 트리 개시에 실패한 작업방은 작업 트리·브랜치·변경사항·재사용 산출물을 먼저 확인한다.
- 보존할 변경이나 산출물이 없으면 사용자에게 다시 묻지 않고 즉시 보관하고, 작업방 목록을 재조회해 기본 목록에서 사라졌는지 확인한다.
- 변경이나 재사용 가능한 산출물이 있으면 삭제·reset·덮어쓰기·보관하지 않고 담당 경로로 보존·재사용·재작성 판단을 요청한다.
- 완료된 PM·개발·검증 작업방은 최종 결과와 보존 증거를 상위 작업방에 전달한 직후 보관한다. 사용자 소통 기준인 기획 작업방은 자동 보관하지 않는다.

### 6.2 필수 작업 기록

작업방에는 최소한 다음을 기록한다.

- 공통 작업 ID와 개발 분류
- 확정 명세와 담당 프로젝트·개발자
- 저장소 URL과 기본 로컬 경로
- 원격 기본 브랜치와 개발 직전 확인한 기준 SHA
- 작업 브랜치와 워크트리 경로
- 허용 파일과 금지 범위
- 완료 조건, 검증 기준, 승인 범위와 중단 조건
- 동시 진행 작업, 충돌 여부, 선행·후행 작업과 병합 순서
- 현재 상태, 마지막 증거와 보존·재사용·재작성 판단

### 6.3 승인 정책

- 사용자가 확정 개발명세의 실행을 지시하면 문서 반영, 작업 생성, 구현, 테스트, commit, Push, PR, PM 검증, 승인된 merge·배포와 운영 기술 확인까지 승인한 것으로 본다.
- 일반 개발과 중요 개발의 수정·테스트·commit·Push·PR은 PM이 전달한 승인 범위 안에서 연속 진행할 수 있다.
- 빠른 처리는 사용자가 확정한 승인 범위를 따른다.
- merge·배포는 승인 범위에 포함되고 PM 판정이 `차단`이 아닐 때만 승인된 경로로 진행한다.
- 삭제, 데이터 구조 변경, 복구가 어려운 작업 또는 승인 범위 밖 병합·배포는 실행 전에 담당 승인 주체에게 보고한다.
- 일반 개발과 중요 개발의 merge·배포 경로는 PM이 관리한다.
- 사용자의 승인 문장은 기획자·PM·개발자에게 원문 그대로 전달하며 내부 담당자의 추정 승인이나 요약 문장으로 대체하지 않는다.
- 승인 범위 안의 Push·PR·merge·배포에서 시스템 권한 요청이 발생하면 대화로 `승인한다`는 답변을 다시 요구하지 않고 실제 권한 승인 요청을 즉시 실행한다.
- 내부 작업방에서 자동 검토가 직접 사용자 승인 근거를 인식하지 못하면 요청을 반복하거나 새 기획 작업방을 만들지 않고 사용자 소통 기준인 기존 기획 작업방에서 실제 권한 요청을 한 번만 실행한다.
- 로그인 비밀번호, 다중 인증, 장치 코드 입력과 운영체제 보안 동의처럼 사용자가 직접 수행해야 하는 인증 절차만 `사용자 조치 필요`로 요청한다.
- 승인 범위 밖 정책 변경, 데이터 손실 위험 또는 복구가 어려운 파괴적 작업은 자동 승인 대상으로 확대하지 않는다.

---

## 7. OneApp Git 기준선과 개발 환경

이 절은 OneApp Git 작업의 공통 단일 원본이다. 역할별 문서에는 아래 절차를 복제하지 않고 권한과 책임 경계만 기록한다.

### 7.1 저장소 기준 정보

- 공통 원격 저장소: `https://github.com/orderzoneapp-coder/oneapp.git`
- 기본 로컬 경로: `C:\Users\USER\Documents\GitHub\oneapp`
- 기본 브랜치: `main`
- 기본 로컬 경로에 다른 작업의 미커밋 변경이 있으면 삭제·reset·덮어쓰기·commit하거나 checkout/switch하지 않는다.
- 기존 저장소가 있으면 다시 clone하지 않는다.
- `main`을 직접 수정하거나 force push하지 않는다.

### 7.2 역할별 저장소 권한

- 기획·PM 작업방은 저장소와 GitHub 원격을 읽기 전용으로 확인한다.
- 기획·PM 작업방에서는 파일 수정, checkout/switch, fetch/pull, commit, Push, merge, 브랜치·워크트리·clone 생성, 원격 추적 ref 갱신과 인증·로그인을 금지한다.
- 브랜치·워크트리 생성, checkout/switch와 원격 추적 ref 갱신은 PM이 지정한 개발자 작업방에서만 수행한다.
- 개발자는 PM이 전달한 저장소·기준 SHA·브랜치·워크트리 범위 안에서만 Git 상태를 변경한다.

### 7.3 최신 기준 SHA 확정

1. PM은 개발 작업방 생성 직전에 GitHub 원격 `main`을 읽기 전용으로 조회해 최신 커밋 SHA를 확정한다.
2. PM은 해당 SHA, 조회 시점, 저장소와 기본 브랜치를 개발자 작업방에 전달한다.
3. 개발자는 지정 개발 환경에서 fetch한 `origin/main`이 PM이 전달한 최신 확인 SHA와 정확히 일치하는지 확인하고 기록한다.
4. 원격 `main`이 더 전진했다면 개발자는 변경 전에 중단해 새 SHA를 PM에게 보고하고, PM이 확정한 최신 SHA를 기준으로 갱신한다.

다음 항목은 작업 기준선으로 사용할 수 없다.

- 로컬 `main`
- fetch하지 않아 오래된 `origin/main`
- 출처와 최신 여부가 확인되지 않은 임의 SHA
- detached HEAD 자체

자동 생성된 워크트리가 detached HEAD이더라도 clean 상태이고 확인된 최신 `origin/main`과 동일하며, 같은 워크트리에서 안전하게 지정 `codex/` 브랜치를 만들 수 있을 때만 개발자 작업방에서 교정한다. 조건을 충족하지 못하면 변경 전에 중단해 PM에게 보고한다.

### 7.4 동일 작업 재사용과 새 작업 생성

- 동일 작업 ID는 기존 브랜치와 기존 전용 워크트리를 재사용한다.
- 동일 작업의 브랜치·워크트리 경로와 기준 SHA를 먼저 확인하고 기록하며 중복 브랜치·워크트리·clone을 만들지 않는다.
- 기존 개발 환경이 dirty이거나 기준 SHA와 안전하게 연결되지 않으면 삭제·reset·덮어쓰기하지 않고 PM에게 보존·재사용 판단을 요청한다.
- 새 작업 ID만 PM이 확정한 최신 SHA에서 `codex/` 작업 브랜치와 전용 워크트리 하나를 생성한다.
- 자동 생성된 유효한 `codex/` 작업 브랜치와 전용 워크트리가 있으면 새로 만들지 않고 그대로 기록·사용한다.

### 7.5 개발 시작 기록과 불일치 처리

개발자는 파일을 수정하기 전에 다음을 기록한다.

- 원격 URL과 기본 브랜치
- PM 확인 SHA와 fetch 후 `origin/main` SHA
- 작업 브랜치와 HEAD
- 워크트리 절대 경로
- clean 여부와 보존해야 할 기존 변경

원격 URL, 기준 SHA, 브랜치, 워크트리 또는 clean 상태가 전달 내용과 다르면 수정하지 않고 PM에게 보고한다. 다른 작업의 변경을 임의로 가져오거나 덮어쓰지 않는다.

### 7.6 GitHub 인증

- 인증은 Push·PR·merge를 실제 담당하는 동일 개발 환경에서만 수행한다.
- 실제 원격 쓰기가 필요한 시점에 `gh auth status`로 한 번 확인한다.
- 같은 환경에서 성공한 증거가 있으면 반복 로그인하거나 다른 작업방에 인증을 넘기지 않는다.
- 브라우저 승인 화면만으로 인증 성공을 판단하지 않는다.
- 기획자와 PM은 인증·로그인을 수행하지 않는다.
- 인증 실패 시 다른 역할이나 작업방에서 로그인 절차를 반복하지 않고 현재 상태를 담당 경로로 보고한다.

---

## 8. 동시 작업, 변경과 중단 통제

### 8.1 독립성·충돌 판단

- PM은 새 개발 작업을 만들기 전에 진행 중인 작업의 작업 ID, 대상 파일, 공통 모듈, 저장구조, 저장키와 배포 예정 범위를 읽기 전용으로 확인한다.
- 같은 파일, 공통 Core, 저장키 또는 동일 업무 흐름을 동시에 변경하면 충돌 작업으로 분류한다.
- 충돌이 없으면 각 작업을 독립적으로 진행한다.
- 충돌이 있으면 PM이 작업 우선순위, 기준 SHA, 선행·후행 관계와 병합 순서를 정한다.
- 정책 우선순위가 필요하면 PM은 기획자에게 판단을 요청한다.
- 기존 작업과 목적·완료 조건이 같은 요청은 새 작업으로 만들지 않고 기존 작업 ID와 환경을 재사용한다.
- 기존 명세 밖의 독립 결과가 필요한 요청만 담당 권한 경로에서 새 작업으로 분리한다.

### 8.2 변경 통제

- 일반 개발과 중요 개발 시작 후 변경 전달은 `사용자 → 기획자 → PM → 개발자` 경로로 진행한다.
- 개발자가 정책 변경이나 범위 확대로 보이는 요구를 받으면 작업을 확대하지 않고 PM에게 보고한다.
- PM이 정책 변경 여부를 판단할 수 없으면 기획자에게 확인한다.
- 기획자는 사용자와 협의해 명세를 갱신하고 PM에게 전달한다.
- PM은 갱신된 명세에 대해 계속 진행, 중단·보존, 재사용 또는 재작성 여부를 결정한다.

### 8.3 중단·보존·재사용·재작성

- 일반 개발과 중요 개발의 중단 여부는 PM이 판단한다.
- 중단 시 개발자는 추가 수정, 테스트, commit, Push, PR과 배포를 멈추고 현재 상태와 증거를 보고한다.
- 중단된 변경은 PM 판단 전 삭제, reset, 덮어쓰기 또는 임의 재사용하지 않는다.
- PM은 변경 파일, 완료 범위, 테스트 결과, 실패 원인, 범위 이탈과 다른 작업 충돌을 검토한다.
- 재사용 시 사용할 변경과 제외할 변경을 명확히 지정한다.
- 재작성 시 기존 변경 보존 방법과 새 기준 SHA를 먼저 확정한다.

---

## 9. 개발·검증·Git·병합·배포 공통 절차

1. 최신 GitHub `main` SHA와 지정 개발 환경의 `origin/main` 일치를 확인한다.
2. 동일 작업은 기존 환경을 재사용하고, 새 작업만 지정 `codex/` 브랜치와 전용 워크트리에서 시작한다.
3. 선언된 허용 범위만 변경하고 다른 작업의 dirty 변경을 보존한다.
4. 개발 분류와 실제 영향 범위에 맞는 직접 테스트·회귀·데이터 검증을 수행한다.
5. 변경 파일, 영향 범위, 기준 SHA와 검증 결과를 확인한 뒤 승인 범위 안에서 명시적 파일만 stage한다.
6. 승인 범위 안에서 commit·Push·PR을 진행하고 브랜치·워크트리·커밋·PR 증거를 기록한다.
7. 일반 개발과 중요 개발은 독립 PM 검증과 `통과 / 사용자 확인 필요 / 차단` 판정을 기다린다.
8. merge·배포가 승인 범위에 포함되고 PM 판정이 `차단`이 아닐 때만 승인된 담당자가 실행한다.
9. 배포 후 승인된 운영 확인 항목을 검증하고 정해진 보고 경로로 전달한다.
10. 실패 시 변경을 임의로 확대하지 않고 기준 SHA, 복구 방법과 남은 위험을 보고한다.

- 최신 `main`에서도 동일하게 재현되는 오류는 현재 작업 실패와 분리한다.
- 기준선 오류는 별도 작업으로 등록하고 현재 범위에서 임의 수정하지 않는다.
- 기준선 오류 때문에 확인하지 못한 항목은 정상으로 추정하지 않고 검증 불가와 남은 위험으로 기록한다.

---

## 10. 위험 기반 검증

- 개발자는 지정 범위의 구현과 직접 기능 테스트에 책임을 진다.
- PM은 모든 기능을 대신 테스트하지 않고 치명적 위험과 범위 이탈을 독립적으로 검증한다.
- 브라우저 자동제어, 실제 파일 반복 업로드와 전체 CI는 모든 작업에 일률적으로 적용하지 않는다.
- 사용자 실사용 확인이 더 효과적인 항목은 PM이 반복 검증하지 않고 기획자에게 확인 항목으로 전달한다.

### 10.1 일반 개발

PM은 다음을 확인한다.

1. 확정 명세와 실제 변경 범위가 일치하는지
2. 범위 밖 파일, 공통 Core와 다른 솔루션을 임의로 변경하지 않았는지
3. 문법검사와 변경 기능의 직접 테스트가 통과했는지
4. 데이터 손실, 운영 중단과 기존 핵심 기능 훼손 위험이 없는지

구체적인 위험 근거가 있을 때만 브라우저 반복검증, 실제 파일 반복 업로드 또는 전체 CI를 추가한다.

### 10.2 중요 개발

실제 영향에 해당하는 다음 핵심 위험만 정밀 검증한다.

- 데이터 보존과 손실 가능성
- 저장 실패와 복구·롤백 가능성
- 공통 저장계약과 연관 앱 영향
- 권한과 승인 경계
- 대표 데이터 계산 결과
- 배포 실패 시 복구 기준

자동검사와 코드 위험 검토를 우선하며 실제 화면 검증은 핵심 업무 흐름의 최소 사례로 제한한다.

---

## 11. PM 판정

PM 판정은 다음 세 가지로 통일한다.

### 11.1 통과

- 범위 이탈과 치명적 위험이 없다.
- 승인된 병합·배포 경로를 진행할 수 있다.

### 11.2 사용자 확인 필요

- 기술적으로 병합·배포 가능한 수준이지만 실제 업무 결과는 사용자의 실사용 확인이 필요하다.
- 이 판정만으로 병합·배포를 차단하지 않는다.
- 기획자가 확인 대상, 방법과 예상 결과를 사용자에게 전달한다.
- 일반 개발은 별도 지정이 없으면 사용자 확인을 다음 승인 작업의 차단 조건으로 사용하지 않는다.
- 중요 개발은 기획자가 완료 조건으로 지정한 사용자 확인만 `운영 확인 대기`로 유지한다.

### 11.3 차단

다음 중 하나면 병합·배포를 중단한다.

- 데이터 손실 또는 복구 불가 위험
- 확정 정책이나 완료 조건과 불일치
- 범위 밖 변경
- 운영 중단 위험
- 공통 Core·저장계약·권한 경계의 승인되지 않은 변경
- 검증 결과만으로 안전성을 판단할 수 없는 중대한 결함

PM은 차단 근거와 가능한 선택지를 기획자에게 보고한다.

---

## 12. 완료와 다음 작업 기준

### 12.1 병합·배포가 승인 범위에 포함된 작업

다음 증거로 완료를 판정한다.

- 승인된 병합 완료
- 배포 성공
- 배포 버전 또는 배포 커밋 일치
- 핵심 화면 접근, 기본 실행 또는 상태 확인 성공

### 12.2 병합·배포가 승인 범위에 포함되지 않은 작업

- 구현·테스트와 결과 보고 완료를 개발 완료로 판정한다.

### 12.3 사용자 확인

- 빠른 처리와 일반 개발의 사용자 실사용 확인은 원칙적으로 사후 확인한다.
- 일반 개발은 완료 증거가 확인되면 사후 사용자 확인과 병행해 다음 승인 작업을 진행할 수 있다.
- 중요 개발은 기획자가 완료 조건으로 지정한 사용자 확인이 있을 때만 확인 전까지 `운영 확인 대기`로 유지한다.
- 별도 사용자 확인 완료 조건이 없으면 PM 핵심 위험 검증과 승인된 배포·운영 기술 확인으로 완료할 수 있다.

---

## 13. 사건 중심 보고와 종료 기록

- 정상 진행 중에는 시간 기준 정기보고를 반복하지 않는다.
- 환경 차단, 범위 확대, 예상 공수 급증, 반복 실패, 개발 완료, 검증 판정과 배포 결과가 발생했을 때 보고한다.
- 동일 실패를 반복하지 않는다.
- 명령이 장시간 진행되면 출력·로그·진행률 또는 프로세스 상태를 확인한다.
- 무응답·정체 또는 상태 확인 불가일 때는 중단하고 현재 상태, 원인과 다음 선택지를 보고한다.
- 빠른 처리는 `개발자 → 기획자 → 사용자`, 일반 개발과 중요 개발은 `개발자 → PM → 기획자 → 사용자` 경로로 보고한다.
- 개발자와 PM은 전체 업무의 최종 완료를 사용자에게 직접 선언하지 않는다.

작업 종료 시 다음을 기록한다.

- 작업 ID와 최종 상태
- 저장소, 기준 SHA, 브랜치와 워크트리
- 커밋과 PR
- 변경 파일과 검증 결과
- 병합·배포 상태
- 재사용·제외 판단과 남은 위험
- 최종 보고 대상

---

## 14. OneApp 저장소 규칙

- MerchOps와 DataOps는 목적과 운영이 분리된 솔루션이다.
- 두 솔루션은 상품, 단가, 재고, 매출 등 자료를 연동한다.
- 공통 데이터 필드나 Core 변경 시 연동 영향을 먼저 검토한다.
- 특정 솔루션 작업 중 관련 없는 다른 솔루션 기능을 임의 변경하지 않는다.
