# AGENTS.md — 공통 실행 규범

Version: 3.0.0
Last Updated: 2026-09-08

## 1. 적용과 문서 경계

- 모든 작업은 사용자의 업무 목적과 마지막 명시적 확정사항을 따른다. 관리자 판단을 지원하고 처리 결과를 검산·보고하며, 시스템 계산을 무조건 정답으로 간주하지 않는다.
- 저장소 업무 규칙은 `사용자 확정 정책·명세 → AGENTS.md → APP_ARCHITECTURE.md → 적용 역할 지침 → 기술 실행계획` 순으로 대조한다. 실제 도구 권한과 실행환경의 안전 제약을 우회하지 않는다.
- 이 문서는 공통 실행 규범, `APP_ARCHITECTURE.md`는 구조·데이터·UI·성능·복구 계약, `app-manifest.json`은 등록된 앱·계약 정보를 관리한다. 작업별 목적·범위·완료조건은 해당 작업 지시·명세에서 관리한다.
- 공수·난이도·모델·작업방 선택은 채팅방의 권고 판단과 작업별 지정에 따른다. 고정된 모델, 프로젝트 ID, 전달 순서나 모든 작업의 PM 경유를 강제하지 않는다.
- 별도 역할 지정이 없으면 현재 요청에 필요한 역할만 수행한다. 분석 요청을 실행 권한으로 확대하지 않는다. 역할 겸임은 가능하나 자체 검증을 독립 검증으로 표시하지 않는다.
- 적용 역할 지침만 참조한다. `roles/PM.md`는 검증·통제 책임이 지정된 경우, `roles/DEVELOPER.md`는 실행 책임이 지정된 경우 사용한다. `roles/PLANNER.md`는 기존과 같이 비활성이다.
- 채팅·워크의 역할별 지침과 작업지시서 원본은 `docs/guidelines/CHATGPT_CHAT.md`, `docs/guidelines/CHATGPT_WORK.md`, `docs/guidelines/TASK_HANDOFF.md`에 보관한다. 해당 역할과 인계에 필요한 문서만 참조하며, 저장소 등록을 ChatGPT 설정의 자동 변경으로 간주하지 않는다.

## 2. 착수와 현재 사실 확인

- 저장소 작업은 최신 원격 `main`과 실제 작업 HEAD, 관련 소스·명세·진행 중 변경을 확인한다. 새 구현은 최신 원격 기준으로 시작하고, 진행 중 작업은 기존 변경을 보존한 채 기준 변경의 영향만 확인한다.
- `AGENTS.md`, 아키텍처의 목적·공통 원칙, 대상 앱의 manifest 항목과 변경에 관련된 상세 계약을 확인한다. 관계없는 앱의 전체 문서·이력을 매번 다시 읽거나 같은 정보를 중복 수집하지 않는다.
- 시작 기록에는 목적, 허용·금지 범위, 실행·검증 책임, 승인 범위, 완료조건과 기준 소스·명세를 짧게 남긴다. 기존 기록이 있으면 재사용하고, 단순 작업에 별도의 장문 명세나 작업방을 요구하지 않는다.
- 문서·소스·manifest·배포 증거가 다르면 현재 사실과 목표를 구분한다. 계획, 과거 완료 보고 또는 테스트 이름만으로 현재 구현·운영 정상 여부를 추정하지 않는다.

## 3. 승인 범위와 자율 실행

- 의견·분석·검토·기획안·수정안 작성은 해당 산출물 작성만 승인한다. 생산 소스 변경, Git 쓰기, 병합·배포 승인으로 해석하지 않는다. 문제 발견도 수정 승인이 아니다.
- 사용자가 확정 명세의 실행을 지시하면 그 명세의 문서 반영·구현·테스트·Git 작업과 승인된 병합·배포·운영 기술 확인까지 진행한다. 사용자가 제외한 단계와 새 정책 판단이 필요한 단계는 보류하며, 같은 승인을 이유 없이 반복해서 받지 않는다.
- 업무 목적·정책·공식 경로·데이터 소유권·권한·정산 기준·복구 불능 위험 수용·완료조건 변경과 범위 확대는 사용자 판단을 받는다. 삭제·데이터 구조 변경·복구가 어려운 작업은 실행 전 영향과 복구 방법을 보고한다.
- 승인 범위 안의 구현·도구·테스트 방법과 안전한 기술적 재시도는 담당자가 결정한다. 기술 문제를 불필요하게 사용자에게 전가하지 않는다.
- 현재 목적에 필요한 최소 변경을 우선한다. 관련 없는 개선, 미래 확장, 신규 공통 엔진, 전체 저장소 이전을 끼워 넣지 않는다. 개별 원인은 해당 앱에서, 확인된 공통 원인은 실제 영향 범위에서 해결한다.
- 역할을 넘길 때 사용자 승인 원문, 최신 명세와 기준 소스·변경 증거를 전달한다. 다른 작업방이 이전 대화·로컬 파일·진행 상태를 자동으로 공유한다고 가정하지 않는다. 인계가 필요하면 다음 작업자가 바로 실행할 수 있는 목적·범위·승인·완료조건·보고 기준의 명령문을 제공한다.

## 4. 데이터와 앱 보호

- 기존 정상 기능·원본·공란·숫자 0·데이터 형식·파일 구조를 임의로 보정·대체·삭제하지 않는다. 승인된 처리 결과는 원본과 비교할 수 있게 한다.
- 앱 독립성, 쓰기 소유권, 공통 UI 책임, 외부 장애 격리와 활성 작업본 보호는 아키텍처 계약을 따른다. 신규 타 앱 원시 저장소 쓰기를 추가하지 않고, 승인된 기존 예외도 범위를 확대하지 않는다.
- 서버 오류·조회 실패·확정 여부 불명을 정상 0건이나 성공으로 바꾸지 않는다. 새 서버 상태가 활성 작업본을 자동 덮어쓰지 않게 하고, 충돌 시 양쪽 자료와 이력을 보존한다.
- 관리자 판단권을 존중하되 인증·권한·데이터 무결성 검사를 해제하지 않는다. 수량 차이·손익·단순 경고를 이유로 관계없는 정상 업무를 차단하지 않는다.
- 문서 간소화, 화면 정리 또는 앱 통합을 데이터 이전·저장계약 변경·공식 주소 변경 승인으로 해석하지 않는다.

## 5. 변경 환경과 복구

- 원격 `main`은 운영 소스 기준이며 직접 수정·직접 푸시·강제 푸시하지 않는다. 변경은 작업 브랜치와 PR로 관리한다.
- 같은 작업의 기존 브랜치와 실행 환경을 재사용한다. 로컬 작업은 다른 변경과 격리하고, 필요한 경우 전용 worktree를 사용한다. 환경 이름이나 경로를 고정 규범으로 삼지 않는다.
- 다른 작업의 미커밋 변경과 산출물을 삭제·reset·덮어쓰기·임의 commit하지 않는다. Git 작업 전 실제 원격·기준 SHA·브랜치·HEAD·작업 경로·변경 상태를 확인하고 자신의 변경만 stage한다.
- 인증은 실제 쓰기를 수행할 환경에서 필요할 때 확인한다. 인증 성공을 이유 없이 반복하지 않으며, 사용자 직접 조작이 필요한 인증만 사용자 조치로 요청한다.
- 롤백은 지정 기준과 호환 계약을 복원하는 작업이다. 신규 기능·리팩터링·별도 오류 수정을 섞지 않고, 이미 확정된 원장·Revision·감사 이력을 소스 롤백과 함께 삭제하지 않는다.

## 6. 검증과 위험 처리

- 실행 담당자는 직접 기능 검증과 변경 영향에 필요한 회귀검사를 수행한다. UI·작업 흐름 변경은 대표 입력부터 기대 결과까지 확인하고, 저장·연동 변경은 관련 실패·충돌·복구를 검증한다.
- 필수 저장소 검사와 아키텍처 최소 검증을 지킨다. 작업방 생략이나 빠른 처리라는 이유로 이를 생략하지 않으며, 같은 증거를 확인하려는 불필요한 전체 재검증도 하지 않는다.
- 검증 책임자가 별도로 지정되면 명세·diff·테스트 증거를 대조하고 실제 위험에 필요한 독립 검증을 수행한다. 작업방 수는 검증 수준을 대신하지 않는다.
- 기준선부터 있던 오류와 이번 변경으로 발생한 오류를 구분한다. 확인하지 못한 항목은 미검증과 남은 위험으로 기록하며, 운영 데이터에 대한 파괴적 테스트를 임의로 수행하지 않는다.
- 데이터 손상, 인증·권한 침해, 복구 불능, 운영 중단, 승인되지 않은 공통 계약 변경 또는 목적·완료조건의 중대한 불일치가 확인되면 해당 위험 작업·병합·배포를 보류한다. 상태와 증거를 보존하고 읽기 중심의 안전한 원인 확인과 보고만 수행한다.
- 범위 밖 발견사항은 근거·영향과 함께 분리해 기록한다. 관계없는 개선을 자동 실행하거나 필요한 근거 없이 모든 업무를 중단하지 않는다.

## 7. 병합·배포와 완료

- 승인 범위와 검증 결과에 따라 실행 담당자가 병합·배포한다. 별도 검증 담당이 지정된 작업은 그 판정을 확인하며, 지정되지 않았다는 이유만으로 중간 작업방을 새로 만들지 않는다.
- 병합 전 변경 파일·PR head·테스트·필수 CI를 대조한다. 그 뒤 코드나 기준이 바뀌면 영향받는 검증을 갱신한다.
- 배포가 범위에 포함되면 병합과 배포 성공을 구분하고, 검증한 버전과 실제 배포 버전의 일치 및 핵심 실행을 확인한다. GitHub Pages와 서버 등 배포 경계가 다르면 변경된 각 경계의 증거를 확인한다.
- 승인 범위의 완료조건 충족을 기준으로 판단한다. 구현 완료·배포 완료·운영 기술 확인·업무검수 완료는 구분하며, 사용자 실사용 판단이 완료조건인 경우에만 사용자 확인 대기로 남긴다.
- 새로운 정책 결정이나 승인이 필요한 상태를 단순 사용성 확인으로 바꿔 배포하지 않는다. 단순 실사용 확인이 남았다는 이유만으로 이미 승인된 안전한 기술 배포를 자동 차단하지도 않는다.

## 8. 보고

- 한국어로 목적을 짧게 정리하고 현재 결론을 먼저 보고한다. 사용자가 붙여넣은 자료를 질문처럼 되풀이하지 않으며, 확인 사실·검토사항·추정을 구분한다.
- 결과에는 목적 충족 여부, 변경 범위, 소스·PR·배포 증거, 직접 검증·증거 검토·미검증의 구분과 남은 위험을 포함한다. 수행하지 못한 실행이나 보유하지 않은 권한을 수행·보유한 것으로 보고하지 않는다.
- 중복 보고와 승인 전달을 줄이고, 범위 변경·차단·반복 실패·완료 등 판단에 필요한 변화를 지정된 보고 경로에 전달한다. 다른 작업방으로의 자동 인계·자동 실행을 증거 없이 선언하지 않는다.
