# AGENTS.md

Version: 1.1.0
Last Updated: 2026-07-26

이 문서는 OneApp 저장소에서 작업하는 Codex 및 AI 개발 도구의 공통 작업 규칙이다.
저장소 루트의 본 문서를 작업 시작 전에 확인하고 적용한다.

## 1. 기본 작업 원칙

- 요청된 작업 범위만 수정한다.
- 관련 없는 기능이나 파일은 임의로 변경하지 않는다.
- 기존 기능, 데이터 형식, 파일 구조를 우선 보존한다.
- 필요한 정보가 부족하거나 목적이 불명확하면 추측하지 말고 확인한다.
- 최소 수정, 최소 영향, 기존 호환성을 우선한다.
- 사용자가 요청하지 않은 기능 확장이나 리팩터링을 하지 않는다.
- 원본 엑셀값, 공란, 0 등 운영 데이터는 임의로 보정하거나 대체하지 않는다.
- 최종 운영 판단은 관리자가 한다.

## 2. GitHub 기준 운영

- GitHub 원격 저장소를 프로젝트의 기준 원본으로 사용한다.
- 로컬 저장소는 임시 작업 사본으로 취급한다.
- 작업 시작 전에 원격 저장소의 최신 상태를 확인한다.
- 원격보다 로컬이 뒤처져 있으면 작업 전에 동기화한다.
- 작업 완료 후 변경사항을 로컬에만 남기지 않고 commit과 push까지 진행한다.
- 다른 PC에서도 이어서 작업할 수 있도록 코드와 관련 문서를 GitHub에 반영한다.
- 원격과 로컬 상태가 다르거나 충돌하면 임의로 덮어쓰지 말고 차이를 먼저 보고한다.
- 작업 중 생성한 임시 인증 파일, 토큰, 비밀번호, 비밀키는 저장소에 커밋하지 않는다.

## 3. GitHub 인증 및 저장소

- 인증이 정상이라면 `gh auth login`을 다시 실행하지 않는다.
- 인증 상태는 `gh auth status`로 한 번만 확인한다.
- 기존 로컬 저장소가 있으면 다시 clone하지 않는다.
- 브라우저 로그인, 장치 인증, 백그라운드 인증 프로세스를 반복하지 않는다.
- 인증이 실패하면 같은 방법을 반복하지 말고 원인과 사용자 조치 방법을 보고한다.
- 저장소 경로와 원격 저장소 주소가 맞는지 먼저 확인한 후 Git 작업을 수행한다.

## 4. 실행 및 오류 처리

- 동일한 실패 명령을 반복하지 않는다.
- 하나의 명령이 30초 이상 진행되면 무작정 기다리지 않는다.
- 30초 이상 걸릴 경우 다음 내용을 보고한다.
  - 현재 상태
  - 지연 원인
  - 진행을 계속해야 하는 이유
  - 다음 선택지
- 사용자의 응답이나 인증 승인을 기다리는 명령은 백그라운드 우회 방식으로 반복하지 않는다.
- 2회 실패하면 현재 방법을 중단하고 다른 해결 방법을 검토한다.
- 불필요한 장시간 대기, 복잡한 우회 명령, 임시 프로세스 반복 생성을 하지 않는다.
- 오류를 숨기거나 성공한 것처럼 보고하지 않는다.

## 5. 승인 및 자동 진행

다음 작업은 별도 승인 없이 연속해서 진행할 수 있다.

- 코드 및 문서 분석
- 파일 검색
- 일반 코드 수정
- 문서 생성 및 수정
- 영향이 작은 버그 수정
- 테스트 및 검증
- commit
- push
- 원격 최신 상태 확인
- 작업 브랜치 생성

다음 작업은 실행 전에 사용자에게 목적과 영향을 보고한다.

- 파일 또는 데이터 삭제
- merge
- 실제 서비스 배포
- 데이터 구조 변경
- 공통 Core 변경
- 대규모 리팩터링
- 폴더 구조의 광범위한 변경
- 복구가 어렵거나 운영 서비스에 직접 영향을 주는 작업
- 다른 솔루션의 동작을 변경하는 작업

## 6. 브랜치 정책

- 문서 수정, 단순 버그 수정, 영향이 작은 변경은 main에 직접 반영할 수 있다.
- 대규모 기능, 공통 Core, 데이터 구조, 삭제, 광범위한 리팩터링은 별도 작업 브랜치를 사용한다.
- 여러 파일 또는 여러 솔루션에 영향을 주는 변경은 작업 브랜치를 우선한다.
- 브랜치를 사용할 경우 main의 최신 상태를 기준으로 생성한다.
- 불필요한 브랜치를 반복 생성하지 않는다.
- 작업 방식은 변경 규모와 복구 난이도를 기준으로 판단한다.

## 7. OneApp 솔루션 관계

- OneApp은 회사 및 공통 개발 기반을 의미한다.
- MerchOps, DataOps, CustomerOps, ControlTower, SmartParser, HistoryViewer 등은 목적과 운영이 구분된 솔루션이다.
- 각 솔루션은 독립적으로 개발·운영되지만 상품, 단가, 재고, 매출, 거래처, 설정값 등 일부 자료를 연동한다.
- 특정 솔루션 작업 중 관련 없는 다른 솔루션 기능을 임의로 변경하지 않는다.
- 공통 데이터 필드, 공통 함수, Apps Script, API, 마스터 구조 또는 Core 변경 전에는 연동 영향을 확인한다.
- 연동 영향이 있으면 변경 대상, 영향받는 솔루션, 호환성 문제를 먼저 보고한다.
- 솔루션별 목적과 업무 흐름을 혼동하지 않는다.

## 8. 기존 코드와 운영 정책

- 기존 기능을 우선 유지한다.
- 기존 코드 구조와 네이밍 방식을 존중한다.
- 필요하지 않은 라이브러리나 프레임워크를 추가하지 않는다.
- 단순한 문제를 해결하기 위해 과도한 추상화나 대규모 구조 변경을 하지 않는다.
- 사용자가 확정한 운영 정책을 임의로 일반적인 방식으로 변경하지 않는다.
- 프로그램이 자동으로 판단한 결과만 정답으로 취급하지 않는다.
- 관리자가 원본 데이터와 전체 작업 내용을 직접 확인할 수 있어야 한다.

## 9. 문서 확인

작업 시작 전에 존재하는 경우 다음 문서를 확인한다.

- `AGENTS.md`
- `APP_ARCHITECTURE.md`
- `app-manifest.json`
- `README.md`
- 해당 솔루션의 개발 명세 또는 운영 문서

문서 내용과 실제 코드가 다르면 임의로 한쪽을 정답으로 정하지 말고 차이를 보고한다.

## 10. 작업 결과 보고

작업 완료 후 다음 항목만 간단히 보고한다.

- 수행한 작업
- 변경한 파일
- 주요 변경 내용
- 테스트 또는 검증 결과
- GitHub commit·push 반영 상태
- 다른 솔루션 또는 공통 데이터에 미치는 영향
- 추가 확인이 필요한 사항

불필요한 전체 로그는 출력하지 않는다.
명령어 출력은 핵심 성공·실패 결과만 요약한다.

## 11. 응답 방식

- 결론과 현재 상태를 먼저 설명한다.
- 불필요하게 긴 설명과 같은 내용의 반복을 피한다.
- 사용자가 직접 해야 하는 작업은 정확한 클릭 위치 또는 명령을 순서대로 안내한다.
- 사용자가 코드를 알지 못한다는 점을 고려해 복사·붙여넣기 가능한 형태로 제공한다.
- 문제 발생 시 원인, 영향, 해결 방법 순으로 설명한다.
- 이미 완료된 인증, clone, 설정 작업을 다시 요청하지 않는다.

## 12. 규칙 변경

- 본 문서는 확정된 공통 운영 규칙이다.
- 작업할 때마다 임의로 내용을 추가하거나 다시 작성하지 않는다.
- 반복되는 문제나 운영 정책 변경이 발생한 경우에만 수정안을 제안한다.
- 사용자의 명시적 요청 또는 승인 없이 본 문서를 변경하지 않는다.
