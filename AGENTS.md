# AGENTS.md

Version: 1.1.0
Last Updated: 2026-07-26

## 1. 기본 작업 원칙

- 요청된 작업 범위만 수정한다.
- 관련 없는 기능이나 파일은 임의로 변경하지 않는다.
- 기존 기능과 데이터 형식을 우선 보존한다.
- 필요한 정보가 부족하면 추측하지 말고 확인한다.
- 최소 수정, 최소 영향, 기존 호환성을 우선한다.

## 2. GitHub 기준 운영

- GitHub 원격 저장소를 프로젝트의 기준 원본으로 사용한다.
- 로컬 저장소는 임시 작업 사본으로 취급한다.
- 작업 시작 전 원격 저장소의 최신 상태를 확인한다.
- 원격보다 로컬이 뒤처져 있으면 작업 전에 동기화한다.
- 작업 완료 후 변경사항을 로컬에만 남기지 않고 commit과 push까지 진행한다.
- 다른 PC에서도 이어서 작업할 수 있도록 결과와 문서를 GitHub에 반영한다.
- 원격과 로컬이 충돌하면 임의로 덮어쓰지 말고 차이를 먼저 보고한다.

## 3. GitHub 인증 및 저장소

- 인증이 정상이라면 `gh auth login`을 다시 실행하지 않는다.
- 인증 상태는 `gh auth status`로 한 번만 확인한다.
- 기존 저장소가 있으면 다시 clone하지 않는다.
- 브라우저 로그인이나 백그라운드 인증을 반복하지 않는다.

## 4. 실행 및 오류 처리

- 동일한 실패 명령을 반복하지 않는다.
- 하나의 명령이 30초 이상 진행되면 중단하고 다음을 보고한다.
  - 현재 상태
  - 지연 원인
  - 다음 선택지
- 2회 실패하면 다른 해결 방법을 검토한다.
- 불필요한 장시간 대기와 우회 작업을 하지 않는다.

## 5. 승인 및 자동 진행

다음 작업은 연속해서 진행할 수 있다.

- 코드 분석
- 파일 검색
- 일반 코드 수정
- 문서 수정
- 테스트
- commit
- push

다음 작업은 실행 전에 사용자에게 보고한다.

- 파일 또는 데이터 삭제
- merge
- 배포
- 데이터 구조 변경
- 공통 Core 변경
- 대규모 리팩터링
- 복구가 어려운 작업

## 6. 브랜치 정책

- 문서 수정, 단순 버그 수정, 영향이 작은 변경은 main에 직접 반영할 수 있다.
- 대규모 기능, 공통 Core, 데이터 구조, 삭제, 광범위한 리팩터링은 별도 작업 브랜치를 사용한다.
- 작업 방식은 변경 위험도에 따라 판단한다.

## 7. OneApp 솔루션 관계

- MerchOps와 DataOps는 목적과 운영이 분리된 솔루션이다.
- 각 솔루션은 상품, 단가, 재고, 매출 등 일부 데이터를 연동한다.
- 특정 솔루션 작업 중 관련 없는 다른 솔루션 기능을 임의로 변경하지 않는다.
- 공통 데이터 필드, 공통 함수 또는 Core 변경 전에는 연동 영향을 확인한다.
- 연동 영향이 있으면 변경 대상과 영향 범위를 먼저 보고한다.

## 8. 작업 결과 보고

작업 완료 후 다음 항목만 간단히 보고한다.

- 수행한 작업
- 변경한 파일
- 주요 변경 내용
- 테스트 결과
- GitHub 반영 상태
- 추가 확인이 필요한 사항

불필요한 전체 로그는 출력하지 않는다.
