name: ORDER Q heading contrast

on:
  pull_request:
    paths:
      - 'orderops/excel-preparation.css'
      - 'orderops/list.html'
      - 'orderops_list.html'
      - 'scripts/test-orderops-heading-contrast-browser.mjs'
      - 'scripts/test-orderops-print-grid-browser.mjs'
      - 'scripts/test-orderops-excel-preparation-browser.mjs'
      - '.github/workflows/orderops-heading-contrast.yml'
  push:
    branches: [main]
    paths:
      - 'orderops/excel-preparation.css'
      - 'orderops/list.html'
      - 'orderops_list.html'
      - 'scripts/test-orderops-heading-contrast-browser.mjs'
      - 'scripts/test-orderops-print-grid-browser.mjs'
      - 'scripts/test-orderops-excel-preparation-browser.mjs'
      - '.github/workflows/orderops-heading-contrast.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  heading-contrast:
    name: Verify ORDER Q heading contrast in actual app
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v7
        with:
          node-version: 24
      - name: Install Korean font for readable evidence
        run: sudo apt-get update -qq && sudo apt-get install -y fonts-noto-cjk
      - name: Check paired heading colors and run existing full browser regression
        env:
          # The current workbench is now the standalone entry. The old canonical
          # NEXUS theme defect is not its baseline; keep all current contrast,
          # sorted/filtered headings and light/dark viewport assertions.
          ORDEROPS_PREPARATION_EVIDENCE_DIR: '${{ runner.temp }}/orderops-heading-evidence'
        run: node scripts/test-orderops-heading-contrast-browser.mjs
      - name: Upload screenshots and measured colors
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: orderops-heading-contrast
          path: '${{ runner.temp }}/orderops-heading-evidence'
          if-no-files-found: error
          retention-days: 7
  print-grid:
    name: Verify ORDER Q paginated print grid
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v7
        with:
          node-version: 24
      - name: Install Korean font for readable print evidence
        run: sudo apt-get update -qq && sudo apt-get install -y fonts-noto-cjk
      - name: Verify real Excel to F9 PDFs, cell borders, colors and data preservation
        env:
          ORDEROPS_PRINT_GRID_BASELINE: '0ff3af7461a335b3201a0752492c78bd88dee1e0'
          ORDEROPS_PREPARATION_EVIDENCE_DIR: '${{ runner.temp }}/orderops-print-grid-evidence'
        run: node scripts/test-orderops-print-grid-browser.mjs
      - name: Upload print PDFs and measured cell borders
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: orderops-print-grid
          path: '${{ runner.temp }}/orderops-print-grid-evidence'
          if-no-files-found: error
          retention-days: 7
